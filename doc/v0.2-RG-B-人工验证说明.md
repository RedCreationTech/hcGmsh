# v0.2 RG-B（Stage 1+2 准出）人工验证说明

> 日期：2026-09-19
> 范围：Stage 1（hc_core + Tree 投影化）+ Stage 2（ProjectStore 抽离）合并准出
> 验证人：用户（Q7 批复：Stage 闸门抽查 15～30 分钟）
> 前置：014 专项评审已于 2026-09-19 单独通过；本闸门重点复核 Stage 2 的保存/加载链路

## 0. 本阶段改了什么

- Stage 1（已评审）：`gmp::core` 核心层 + ModelTreeAdapter（Tree 退为投影，双写并存）；
- Stage 2（本次新增）：`ProjectStore`（`include/gmp/ProjectStore.h` + `src/ProjectStore.cpp`）接管工程文件的全部 YAML 读写、schema 版本闸门、类型 coercion、去重、网格路径迁移（legacy out/ 与他项目 .work/case 两种）、另存为迁移。MainWindow 只保留 UI 联动（面板 apply、状态栏、最近项目、模块刷新等）。MainWindow 净减约 270 行。

自动化证据：新增 `test_project_store_contract` 服务级测试（round-trip 不动点、4 条加载拒绝路径、两类迁移、自有/外部路径不动）；CTest 193 条断言全绿；全量巡览 114 步全绿；真实 G1 项目等价判据通过（Document==Tree、两次同步 `.i` 逐字一致）；019/024/G0 隔离语义合同全部定向通过。

## 1. 验证步骤（约 20 分钟）

### 1.1 构建与启动

```bash
cd /Users/a123/Desktop/code/Gmsh-moose-parview
git pull  # 应到 93f0e87 或更新
cmake --build build -j4
./build/gmp_ise
```

### 1.2 保存/加载主干（ProjectStore 抽离的直接覆盖区）

1. 打开 G1 项目 `phase5-g1-assembly-contact.gmp.yaml`：标题栏正确、`Workflow ready`、装配预览与物理组清单正常；
2. 保存项目：提示正常，重开后一切保持；
3. 同步 `.i` 两次：逐字一致（重点 `[Contact]` / `[BCs]` / `[Materials]` / 单一 `[Executioner]`）；
4. 生成报告：全对象可追溯（Materials/Section/Physics/BC/Contact/Step/Outputs 来源行齐全）。

### 1.3 另存为与迁移（024 语义复核）

1. 另存为临时副本（如 `/tmp/v02-rgb-copy.gmp.yaml`）：标题栏切换；副本 Mesh 路径迁移到副本自有 `.work/case/v02-rgb-copy/`；同步 `.i` 的 `[Mesh/file]` 指向副本路径；
2. 关闭副本，重开主项目，确认主项目网格路径未被污染；
3. 删除临时副本目录。

### 1.4 新建项目隔离（G0 语义复核）

新建临时项目：MOOSE 设置的输入文件/工作目录/网格路径应为空，不继承 G1 项目；关闭不保存。

### 1.5 兼容性与边界

1. 打开一个**旧项目**（Phase 4 验收项目或其他既有 `.gmp.yaml`）：应正常加载，无版本弹窗以外的提示；
2. 性能观感：打开 G1 / 保存 / 同步 `.i` 与基线（≈30ms / ≈20ms / ≈15ms）无体感差异。

## 2. 通过标准与回填

- 全部正常 → 回复“RG-B 通过”，我回填记录并进入 Stage 3（MooseInputGenerator 抽离）；
- 任何异常 → 发我现象（截图/日志），登记差异修复后重新申请 RG-B。

## 3. 相关提交

- Stage 1：`b326343`（core 骨架）、`078be97`（图）、`7eb65e2`（事务）、`2ea6d09`+`87ffc7c`（投影适配器）
- 评审期缺陷修复：`81a2ff8`+`a3c5593`（025/026）
- Stage 2：`f908d42`（ProjectStore 层+单测）、`e2e54fb`（MainWindow 接线）、`93f0e87`（执行记录）
