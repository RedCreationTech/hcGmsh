# UI 重构 Phase 1 人工验收清单

> 当前验收范围：L-01～L-05 全部通过；Phase 1 已完成
> 前置条件：Phase 0 已通过；应用能够正常启动
> 结论规则：每个任务对应的人工用例全部通过后，才可提交并进入后续任务。
> 验收日期：2026-09-02
> 验收结论：2026-09-02 用户确认 Phase 1 全部人工用例通过，可以进入 Phase 2。

## TEST-P1-L01-01 两栏布局与舞台占比

1. 启动应用并关闭所有浮动窗口。
2. 将主窗口内容区调整为约 1440×900。
3. 确认主内容区只有左侧模型树和中央舞台，不存在永久右侧属性区域。
4. 确认左树默认宽度下，舞台宽度不低于主内容区的 75%。
5. 横向放大主窗口，确认新增宽度优先分配给中央舞台。

通过标准：无右侧占位；中央舞台始终可见且满足宽度要求。

## TEST-P1-L01-02 兼容工作窗入口与树选择行为

1. 保持 Module Workspace 关闭。
2. 单击模型树中的 Material、Step、Load 等节点。
3. 确认树选择和模块上下文发生同步，但不会自动弹出 Module Workspace；Sketches 子节点同样遵循单击只选择。
4. 显式点击顶部 Material、Step、Load 等模块页签，确认工作窗打开、置前并显示对应页面。
5. 关闭工作窗后，通过 View → Module Workspace 再次打开。

通过标准：树单击不打断舞台操作；两种显式入口均可恢复工作窗；切换页签时页面对应正确。

## TEST-P1-L01-03 浮动窗口与舞台独立性

1. 打开 Module Workspace，将其拖到主窗口右上方或第二块屏幕。
2. 调整工作窗大小，并依次切换 Part、Mesh、Job、Visualization、Results。
3. 关闭工作窗，确认中央舞台没有留下空白占位。
4. 再次打开工作窗，确认其为非模态窗口，主窗口和舞台仍可点击操作。

通过标准：工作窗可移动、缩放、关闭和恢复；关闭不销毁页面；舞台不被永久挤占。

## TEST-P1-L01-04 布局状态恢复

1. 调整左侧模型树宽度和底部 Console 高度。
2. 调整 Module Workspace 的位置与大小，并保持其打开。
3. 正常关闭后重新启动应用。
4. 确认主窗口尺寸、左栏宽度、Console 高度、工作窗位置/大小和可见状态均恢复。
5. 关闭工作窗后再次正常退出并启动，确认工作窗保持关闭。

通过标准：两次重启后的布局状态分别与退出前一致，且不存在屏幕外无法找回的窗口。

## 验收记录

| 用例 | 结果 | 备注 |
|---|---|---|
| TEST-P1-L01-01 | 通过 | 两栏布局、舞台占比和空间分配符合预期 |
| TEST-P1-L01-02 | 通过 | 树选择及显式工作窗入口符合预期 |
| TEST-P1-L01-03 | 通过 | 浮动窗口与舞台保持独立 |
| TEST-P1-L01-04 | 通过 | 布局状态可正常保存与恢复 |

## TEST-P1-L02-01 中央舞台始终显示

1. 启动应用，依次进入 Visualization 和 Results 模块。
2. 分别打开 Plot、Table 和 Results 页面。
3. 在各工作窗之间切换并调整其位置。

通过标准：中央区域始终为 3D Viewport；Plot/Table/Results 不会替换或卸载舞台。

## TEST-P1-L02-02 Job 独立工作窗

1. 显式点击 Job 模块页签，确认打开 Job Workspace。
2. 检查作业列表、MOOSE 参数、输入编辑器和运行控制仍可访问。
3. 启动一个可持续数秒的测试作业，在运行期间关闭 Job Workspace。
4. 通过 View → Job Workspace 重新打开。

通过标准：窗口关闭后作业继续运行；重新打开后状态、日志和控制仍然存在。

## TEST-P1-L02-03 Visualization 与 Results 独立工作窗

1. 显式点击 Visualization，确认详细显示控制在 Visualization Workspace 中。
2. 使用“显示曲线预览”和“显示表格预览”，确认 Results Workspace 分别切换到 Plot/Table。
3. 显式点击 Results，确认打开同一个 Results Workspace 的 Results 页。
4. 加载一个 `.e` 或 `.msh` 结果，在 Results 中选择“在视图中打开”。

通过标准：三个入口使用单实例工作窗；Viewer 数据正常更新，中央舞台不切页。

## TEST-P1-L02-04 工作窗恢复与旧入口回归

1. 调整 Job、Visualization、Results 三个窗口的位置与尺寸。
2. 关闭其中一个窗口，保留另外两个打开，正常退出并重启应用。
3. 确认各窗口的位置、尺寸和可见状态分别恢复。
4. 依次巡览 Part、Material、Section、Assembly、Step、Interaction、Load、Sketch、Mesh，确认兼容页仍可打开。

通过标准：独立窗状态恢复正确；旧模块入口和表单未因迁移失效。

## L-02 验收记录

| 用例 | 结果 | 备注 |
|---|---|---|
| TEST-P1-L02-01 | 通过 | 中央舞台保持为唯一中央内容 |
| TEST-P1-L02-02 | 通过 | Job 工作窗关闭不影响作业状态 |
| TEST-P1-L02-03 | 通过 | Visualization、Results、Plot、Table 独立展示正常 |
| TEST-P1-L02-04 | 通过 | 工作窗状态恢复及旧入口回归正常 |

> L-02 验收日期：2026-09-02
> L-02 验收结论：全部通过，L-02 完成，可以进入 L-03。

## TEST-P1-L03-01 舞台工具栏布局与折叠

1. 将主窗口调整为 1280×720。
2. 确认工具栏贴在舞台左缘，宽度约 42 px，不形成新的大面积侧栏。
3. 若高度不足，确认仅工具栏内部出现纵向滚动，不挤压或裁断中央舞台。
4. 点击顶部折叠按钮，再次点击展开。

通过标准：折叠后只保留展开按钮；展开后恢复原工具，舞台没有明显尺寸跳动。

## TEST-P1-L03-02 视图导航与显示工具

1. 加载一个 `.msh` 或 `.e` 数据集。
2. 分别选择旋转、平移、缩放模式，在舞台中使用左键拖动。
3. 依次点击适配、前视、右视、顶视和轴测按钮。
4. 点击显示方式、拾取、清除选择和剖切按钮。

通过标准：场景按当前模式响应；互斥模式有明确选中态；拾取/剖切状态与 Visualization Workspace 同步。

## TEST-P1-L03-03 模块上下文工具

1. 切换 Part、Sketch、Mesh、Visualization、Results 模块。
2. 确认通用导航按钮始终存在。
3. 在 Sketch 中确认出现选择、直线、圆、圆弧、矩形、删除工具，并与草图面板选中态同步。
4. 在 Mesh 中确认出现工作窗与生成入口；在 Visualization/Results 中确认出现对应独立工作窗入口。

通过标准：模块工具只在对应上下文显示，切换后无上一个模块的残留按钮。

## TEST-P1-L03-04 Esc 与回归

1. 开启拾取、剖切并选择平移或缩放模式。
2. 按 `Esc`。
3. 在 Sketch 编辑中选择绘制工具，再按 `Esc`。
4. 关闭/打开 Visualization Workspace，确认舞台工具仍可使用。

通过标准：`Esc` 恢复旋转模式并关闭临时拾取/剖切；Sketch 恢复选择工具；独立工作窗不影响工具栏状态。

## L-03 验收记录

| 用例 | 结果 | 备注 |
|---|---|---|
| TEST-P1-L03-01 | 通过 | 工具栏布局、滚动和折叠符合预期 |
| TEST-P1-L03-02 | 通过 | 视图导航、显示、拾取和剖切符合预期 |
| TEST-P1-L03-03 | 通过 | 通用及模块上下文工具切换正确 |
| TEST-P1-L03-04 | 通过 | Esc、工作窗及舞台工具状态回归通过 |

### TEST-P1-L03-05 缺陷 2026-09-02-001 定向回归

1. 启动应用，确认舞台左侧工具已显示可辨识图标，不再出现 `R/P/Z/S/F/Fr/Rt/T/I/D/C` 字母占位。
2. 不加载数据，依次点击适配、拾取、显示方式和剖切，确认状态栏或消息区明确提示需要加载的数据类型，应用不退出。
3. 加载一个 `.msh`，连续点击三次“显示方式”，确认依次出现表面、线框、表面 + 边线；开启拾取后点击网格，再清除选择；开启/关闭剖切。
4. 若有 `.e` 结果，开启选择按钮，确认进入 Probe 而非网格 Pick；切换显示方式后结果表示发生变化。
5. 点击顶部“部件”页签，再次点击已选中的“部件”；随后依次选择模型树 Parts 根节点和任一 Part 子节点。
6. 重复步骤 5 三次，观察终端没有 `std::length_error`，应用不退出；模块工作窗仍可关闭和再次打开。

通过标准：图标、命令映射和反馈符合预期；“部件”页签及 Parts 树节点重复操作不崩溃。若仍退出，请保留崩溃前最后一次点击对象及完整终端日志，缺陷继续保持“回归中”。

### TEST-P1-L03-06 缺陷 2026-09-02-002 草图编辑入口回归

1. 进入“草图”模块，点击“新建草图”。
2. 确认新节点出现在 Sketches 下，中央舞台进入 2D 草图模式，同时弹出并置前草图编辑工作窗。
3. 确认编辑内容从工作窗顶部开始，包含当前草图名称、绘图工具、几何约束、尺寸、撤销/重做和“完成编辑”；窗口高度不足时可以滚动编辑内容，但不需要先滚过草图管理列表。
4. 点击“完成编辑”或关闭工作窗，确认草图被保存、工作窗关闭；模型树仍选中该草图，舞台退出可修改的编辑态，但继续以 2D 只读预览显示该草图。
5. 单击模型树中的另一个草图，确认只同步选中项和“草图”上下文，不自动弹窗。
6. 双击该草图，确认同一个草图编辑工作窗打开并显示双击的草图名称；关闭后重复双击一次。
7. 打开草图工作窗，在草图管理列表中双击一个条目，确认进入草图编辑，而不是跳到“属性”模块。

通过标准：四个显式入口（新建、树双击、列表双击、“打开编辑”）行为一致；单击不弹窗；完成编辑后数据保存且当前草图不会从舞台消失；离开 Sketch 模块后才恢复原 3D 场景。

### TEST-P1-L03-07 缺陷 2026-09-02-003 草图预览与部件弹窗回归

1. 新建或双击一个草图，在编辑工作窗中绘制一条线或一个圆，点击“完成编辑”。
2. 确认模型树仍选中该草图，舞台中的线/圆仍可见；在舞台点击或按 Delete，确认只读预览不会修改草图。
3. 单击另一个草图，确认舞台切换为该草图的只读预览；双击后确认重新进入可编辑状态。
4. 从 Sketch 切换到 Part、Mesh 或 Results，确认草图预览被清除并恢复该模块原有的 3D 舞台内容。
5. 在已有草图的前提下点击顶部“新建部件”，选择来源草图并确认；确认新部件节点名称不与现有节点重复，且立即显示并置前 `Part Editor — <部件名>` 工作窗。
6. 关闭工作窗后双击模型树任一 Parts 子节点，确认再次显示同一部件工作页；在部件管理列表双击或点击“打开所选部件”也应得到相同行为。

通过标准：完成编辑只终止写操作、不清空当前草图；只读预览不可误编辑且离开 Sketch 后正确释放；部件的新建、树双击、列表双击和“打开所选”均统一打开部件工作窗。

## L-03 最终验收结论

| 用例 | 结果 | 备注 |
|---|---|---|
| TEST-P1-L03-05 | 通过 | 图标、命令映射及 Parts 稳定性回归通过 |
| TEST-P1-L03-06 | 通过 | 草图四个显式编辑入口回归通过 |
| TEST-P1-L03-07 | 通过 | 草图只读预览与部件工作窗入口回归通过 |

> L-03 验收日期：2026-09-02
> L-03 验收结论：用户确认全部人工验证通过；L-03 完成，可以进入 L-04。

## TEST-P1-L04-01 顶部三层结构与紧凑高度

1. 启动应用并将主窗口调整到 1280×720。
2. 从上到下确认顶部只有“菜单栏 → 常用图标工具组 → 工作上下文条”三层，不再显示 13 个等宽模块页签。
3. 确认菜单包含 File、Model、View、Mesh、Job、Tools、Settings、Help（中文界面显示对应译名）。
4. 确认三层合计高度不超过约 100 px，模块、项目、当前对象三个关键字段完整可见，中央舞台未被明显压缩。

通过标准：顶部层级和菜单完整；1280 px 宽度下无关键字段截断、重叠或额外模块页签行。

## TEST-P1-L04-02 图标工具分组与共享命令

1. 观察顶部图标区，确认项目、编辑、建模、网格、作业五组命令有清晰间隔。
2. 将鼠标停在保存、撤销、生成网格、运行、停止等图标上，确认 Tooltip 与菜单命令含义一致，快捷键仍可见或可从菜单确认。
3. 分别通过菜单、图标按钮和快捷键执行一次保存；确认三种入口行为一致。
4. 编辑草图后分别通过顶部撤销图标和 `Ctrl/Cmd+Z` 撤销，再通过重做恢复；确认按钮启用状态与草图撤销栈一致。

通过标准：工具按钮与菜单复用同一状态和执行结果；不存在同名命令行为分叉。

## TEST-P1-L04-03 模块与项目上下文

1. 使用“模块”选择器依次切换 Sketch、Part、Material、Mesh、Job、Visualization、Results。
2. 确认每次切换后模块命令、舞台左侧上下文工具和对应工作窗均同步更新。
3. 新建未保存项目时确认“项目”显示“未命名”；打开或保存项目后确认显示当前项目文件名且保持只读。
4. 在中英文之间切换，确认模块、项目和当前对象字段都正确翻译且布局不跳动。

通过标准：模块选择器完整替代旧页签；项目字段准确且不可编辑；中英文切换不破坏上下文。

## TEST-P1-L04-04 当前对象双向同步

1. 进入 Part 模块，在模型树选择不同 Parts 子节点，确认“当前对象”同步显示对应名称。
2. 从“当前对象”下拉框选择另一个部件，确认模型树自动定位并选中该节点。
3. 对 Sketch、Material、Step、Mesh、Job、Results 至少各验证一次；无对象时确认显示“未选择”。
4. 新建、复制、重命名或删除节点后确认下拉选项立即刷新，没有失效或重复引用。

通过标准：树与工作上下文保持双向一致；对象集合随模块和 CRUD 实时刷新；无对象状态明确。

## L-04 验收记录

| 用例 | 结果 | 备注 |
|---|---|---|
| TEST-P1-L04-01 | 通过 | 顶部三层结构、菜单完整性和紧凑高度符合预期 |
| TEST-P1-L04-02 | 通过 | 五组图标工具与共享命令行为一致 |
| TEST-P1-L04-03 | 通过 | 模块、项目和当前对象上下文同步正确 |
| TEST-P1-L04-04 | 通过 | 模型树与当前对象选择器双向同步正确 |

> L-04 验收日期：2026-09-02
> L-04 验收结论：用户确认全部人工验证通过；L-04 完成，可以进入 L-05。

## TEST-P1-L05-01 默认布局与四向停靠

1. 启动应用，执行 View → Reset Tool Layout（视图 → 恢复默认工具布局）。
2. 确认 Project、Edit、Model、Mesh、Job 五个工具组排列在顶部，每组左侧有可见拖拽手柄；Display Group（显示工具组）以紧凑小窗悬浮在舞台右上区域，且不遮挡左下角方向标。
3. 从手柄拖动 Project 组，使其依次悬浮并停靠到主窗口顶部、底部、左侧和右侧；接近合法边缘时确认出现系统停靠预览，释放后整组吸附。
4. 将 Display Group 拖入顶部已有工具组之间，确认可以与其他工具组排列在同一行，而不是被强制放到第二行。
5. 将 Project 组拖回顶部，确认组内按钮顺序未变化，舞台仍可使用。

通过标准：六个工具组均可整组移动；四向停靠和悬浮有效；默认 Display Group 位置正确；没有新增永久侧栏。

## TEST-P1-L05-02 Display Group 命令与舞台交互隔离

1. 加载一个 `.msh`，依次点击 Display Group 中的“切换显示方式、拾取、清除选择、剖切”。
2. 确认四个按钮分别复用舞台已有命令；拾取、剖切的选中态与舞台左侧工具栏同步，空数据或不支持状态仍给出明确提示。
3. 开启拾取，记录当前相机角度；按住任一工具组手柄，将工具组从舞台上方拖过后释放。
4. 确认拖动过程中相机没有旋转/平移、没有意外拾取对象；释放后拾取状态恢复。
5. 点击 Display Group 标题栏附近的非按钮区域，确认下方 VTK 舞台没有误触发；小窗内容之外不存在覆盖舞台的大面积透明区域。

通过标准：显示组按钮不是占位操作；工具组拖动与 VTK 相机/拾取隔离，释放后状态正确。

## TEST-P1-L05-03 View 菜单显隐与默认恢复

1. 打开 View → Toolbars（视图 → 工具栏），确认 Project、Edit、Model、Mesh、Job、Display Group 共六项均存在且勾选状态与实际可见状态一致。
2. 取消勾选 Model，确认该组立即隐藏；再次勾选，确认恢复显示。
3. 随意移动两个工具组并隐藏一个工具组，然后执行“恢复默认工具布局”。
4. 确认五个常用组回到顶部、全部重新显示，Display Group 回到舞台右上预置位置。

通过标准：菜单与可见状态双向一致；默认恢复一次完成且不影响项目数据。

## TEST-P1-L05-04 布局持久化与越界恢复

1. 将 Project 组停靠到底部、Edit 组隐藏、Display Group 移动到另一个可见位置，正常关闭应用后重新启动。
2. 确认三个布局状态均按退出前恢复。
3. 若有第二屏，将一个悬浮组放到第二屏后关闭应用，断开第二屏再启动；若可调整系统缩放，也可在改变缩放后重启。
4. 确认原本越界的工具组被移回当前屏幕可见区域，仍能抓住标题/手柄移动。
5. 执行“恢复默认工具布局”，正常关闭并再次启动，确认默认布局被持久化。

通过标准：位置、停靠方向和显隐状态可跨重启恢复；屏幕/DPI 变化后工具组不会永久丢失；重置结果也可持久化。无第二屏且无法改变缩放时，步骤 3～4 可记为“环境不具备”，不阻断其余项验收。

## TEST-P1-L05-05 L-03/L-04 回归

1. 将窗口调整为 1280×720，切换 Sketch、Part、Mesh、Visualization、Results。
2. 确认顶部三层结构、模块/项目/当前对象、舞台左侧工具栏和中央舞台均正常。
3. 重复新建/双击草图、完成编辑、双击部件的既有路径。
4. 确认无异常退出，草图预览、部件工作窗和 Job/Results 独立工作窗行为不变。

通过标准：L-05 没有破坏已通过的 L-03、L-04 交互与稳定性。

## L-05 验收记录

| 用例 | 结果 | 备注 |
|---|---|---|
| TEST-P1-L05-01 | 通过 | 工具组可整组拖拽、浮动并四向停靠 |
| TEST-P1-L05-02 | 通过 | Display Group 默认位置、命令反馈和舞台操作正常 |
| TEST-P1-L05-03 | 通过 | View 菜单显隐和恢复默认布局正常 |
| TEST-P1-L05-04 | 通过 | 布局持久化与越界恢复正常 |
| TEST-P1-L05-05 | 通过 | L-03/L-04 交互与稳定性回归通过 |
| TEST-P1-L05-06 | 通过 | 两个图标可辨识，Display Group 可与其他工具组同排停靠 |

> L-05 / Phase 1 验收结论：2026-09-02 用户确认全部人工验证通过；L-05 与 Phase 1 关闭，进入 Phase 2 / I-01。

### TEST-P1-L05-06 缺陷 2026-09-02-004 图标辨识与显示组同排停靠

1. 观察顶部 Project 组的“新建项目”和 Mesh 组的“生成网格”，确认新建项目显示“折角文档 + 蓝色加号”，生成网格保持网格图案，二者不再近似。
2. 分别悬停两个图标，确认 Tooltip 为“新建项目”和“生成网格”，点击后仍执行各自原命令。
3. 执行“恢复默认工具布局”，确认 Display Group 默认悬浮；将其拖到顶部 Project/Edit/Model/Mesh/Job 所在行的组间空隙。
4. 确认停靠预览出现在现有工具栏行，释放后 Display Group 与其他组同排；可在同一行继续左右调整位置。
5. 再将 Display Group 拖出为浮动组，然后拖回顶部同一行，确认两次均不被强制放到第二行。

通过标准：新建项目与生成网格图标可一眼区分；Display Group 使用统一工具栏停靠体系，可在顶部第一行与其他组混排。
