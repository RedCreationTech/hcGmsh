# v0.2 RG-D 总准出人工复核说明（v0.2 最终验收）

> 日期：2026-09-19
> 范围：v0.2 全部 7 个 Stage 完成后的总准出（Q7 批复：总准出做完整人工复核）
> 验证人：用户
> 预计耗时：45～60 分钟
> 自动化基线：全量真实点击巡览 114 步全绿；CTest `gmp_ise_phase0` 全绿（含 10 个服务级合同用例）；真实 G1 项目 `.i` 与基线逐字节一致；快照 manifest 除时间字段外逐字段一致
> 复核结论：**✅ RG-D 通过（2026-09-20，用户确认）**
> 冻结标记：`v0.2-rc1`

## 0. v0.2 完成了什么（一句话版）

在**零功能新增、零行为变化**（以逐字一致与 114 步巡览为证）的前提下，完成了架构地基重构：

- `gmp::core`：ProjectDocument/ObjectId/PropertyBag/DependencyGraph/TransactionManager；模型树退为投影；
- `ProjectStore`：工程读写/迁移出 MainWindow；
- `MooseInputGenerator` + `SnapshotService`：`.i` 生成与快照出 MainWindow/MoosePanel；
- `PhysicalGroupService`/`GmshMesher`/`AssemblyGeometryService`：Gmsh 引擎逻辑出 GmshPanel；
- VtkViewer 拆分为 Sketch/Mesh/Result 三视口 + 共享底座；
- stale 传播切换为 DependencyGraph 驱动；CRUD/表单提交经事务层审计；
- CMake 模块化：`hc_core`/`hc_simulation`/`hc_mesh` 独立库，测试目标不再重复编译。

## 1. 复核步骤

### 1.1 干净构建（验证 CMake 模块化与交付能力）

```bash
cd /Users/a123/Desktop/code/Gmsh-moose-parview
git pull  # 应到 c45dc95 或更新
cmake --build build -j4
ctest --test-dir build --output-on-failure
mkdir -p /tmp/gmp-ui-tour && GMP_TOUR_REAL_CLICKS=1 GMP_SCREENSHOT_DIR=/tmp/gmp-ui-tour ./build/gmp_ise
# 期望：ctest 100% passed；巡览 exit=0 无 FAILED（可选，若你已看过最近全绿记录可跳过巡览）
./build/gmp_ise
```

### 1.2 G1 全流程走查（约 20 分钟，v0.2 零回归红线的最终确认）

1. 打开 `phase5-g1-assembly-contact.gmp.yaml`：标题栏、Workflow ready、装配预览、物理组清单 8 组；
2. 属性表单：双击 `concrete_elasticity`，确认弹性模量显示 `29791.45978`（025）、高级参数 youngs_modulus tooltip 含 Pa/MPa 单位（026）；修改→取消不写入；修改→确定写入并恢复；
3. “构建装配”：预览正常；随后选中 `mesh_assembly_g1` 重新“生成 3D 网格”：175086 节点 / Hex8=152950 / 8 组单元数回基线（计时 ≈3s 级）；
4. 同步 `.i` 两次：逐字一致；生成报告全对象可追溯；
5. 导出 contract v2 快照：manifest 完整、包内 `.i` 网格为相对文件名、哈希一致；
6. 保存 → 关闭 → 重开：一切保持（019 语义：不点构建装配直接校验 ready）。

### 1.3 项目生命周期复核（覆盖 021/024/027）

1. 打开 G1 项目 → 新建临时项目：舞台清空无残留（027）、MOOSE 设置路径为空（G0 隔离）；关闭不保存；
2. 打开 G1 项目 → 另存为 `/tmp/v02-final-copy.gmp.yaml`：副本网格路径迁移到副本自有工作目录（024），同步 `.i` 的 `[Mesh/file]` 指向副本；关闭副本并删除；
3. 重开主项目：网格路径未污染（SHA-256 `f87c1606…` 不变）。

### 1.4 交互质感走查（视口拆分覆盖区，约 10 分钟）

1. 舞台左侧工具栏：旋转/平移/缩放/适配/前右顶轴测视图切换；
2. 显示模式（实体表面/线框/表面+边线）与显示/隐藏/隔离；
3. 物理组拾取：候选三列、黄色高亮、关闭后舞台恢复；
4. 草图：进入任一草图编辑，绘制/约束/完成编辑全流程；
5. 结果视图：加载任一 Exodus 结果（如 tests/fixtures 或旧项目结果），变量切换、时间轴播放、切片；
6. 工具组拖拽到舞台右上角再拖回；窗口最大化/还原。

### 1.5 性能对照（基线 §5）

| 操作 | 基线 | 本次 |
|---|---:|---:|
| 打开 G1 项目 | ≈30 ms | 应瞬时 |
| 生成 3D 网格 | ≈3.2 s | 应秒级（≈3s） |
| 同步 `.i` | ≈15 ms | 应瞬时 |
| 保存项目 | ≈20 ms | 应瞬时 |

### 1.6 v0.2 准出 11 项确认（自动化已自检 10/11，你确认体感即可）

核心层独立、Tree 为投影、核心测试无 Widget、save/load 与 block 生成出 MainWindow、Assembly/Mesh 不依赖 GmshPanel Widget、统一 DependencyGraph、基础 Transaction 层（Q4 批复：Undo/Redo 推迟 v0.3）、G1 四要素回归、schema v2 兼容、hc_core/hc_simulation 独立目标。

## 2. 通过标准与回填

- 全部正常 → 回复“RG-D 通过”，我回填记录、打 `v0.2-rc1` 闸门 tag（Q1 发版纪律），v0.2 关闭；
- 任何异常 → 发我现象，登记差异修复后重新申请 RG-D。

## 2.1 最终回填

- 2026-09-20：用户确认按推荐产品顺序执行，RG-D 人工总准出通过。
- v0.2 关闭；冻结 tag 为 `v0.2-rc1`。

## 3. 全量提交索引

- Stage 0：`0b8cd44/fae055a/d0fdd5e/5163535`；Stage 1：`b326343/078be97/7eb65e2/2ea6d09/87ffc7c`；
- Stage 2：`f908d42/e2e54fb`；评审期缺陷：`81a2ff8`（025/026）、`5c423b1`（027）；
- Stage 3：`f3e13b6/d4d3f3e/a80a61e`；Stage 4：`b6b6364/00e21e9/946b958`；
- Stage 5：`c325f13/9371292`；Stage 6：`3e83be7/20842c9`；Stage 7：`abde4ef`。
