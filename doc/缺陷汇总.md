# 缺陷汇总

## 2026-09-02-001 舞台工具栏占位图标、部分命令无响应及点击部件异常退出

- **影响接口/模块**：Phase 1 / L-03；`StageLeftToolbar`、`VtkViewer` 舞台命令、模块页签与模型树点击路径。
- **现象**：舞台左侧按钮仅显示 `R/P/Z/S/F...` 等开发期字母；加载网格后部分按钮没有可见响应；用户点击“部件”后进程曾以 `std::length_error: vector` 未捕获异常退出。启动日志中的 `vtkOrientationMarkerWidget` 与 macOS IMK 警告先于异常出现，但现有证据不足以将其认定为直接根因。
- **复现步骤**：
  1. 启动 `./build/gmp_ise`，观察舞台左侧工具栏。
  2. 加载 `.msh`，点击“显示方式”、拾取、剖切及视图按钮。
  3. 点击顶部“部件”页签；重复点击当前“部件”页签；选择模型树 `Parts` 根节点和部件子节点。
- **根因**：
  1. L-03 实现错误地把字母占位延后到 Phase 3，违反 L-03 已明确的“图标为主”验收要求。
  2. “显示方式”统一修改 Exodus 使用的 `repr_combo_`；`.msh` 模式下该控件被禁用，实际网格显示由 `show_faces_` / `show_edges_` 控制，因此命令对网格无效。
  3. 空舞台或模式不支持时命令静默返回，没有禁用原因或状态反馈，造成按钮失效观感。
  4. VTK 左键观察器在正常交互样式已接收事件的同时，手动再次调用 `OnLeftButtonDown()`，存在重复事件分发和重入风险。该问题已确认不安全并修复；但用户报告的单次 `std::length_error` 在当前环境尚未稳定复现，不能把它声明为唯一直接根因。
  5. 原 19 步 GUI 巡览仅调用 `setCurrentIndex()`，未发出真实 `tabBarClicked`，也未覆盖模型树 Parts 选择，导致点击路径回归缺口。
- **关联逻辑/路径**：`src/StageLeftToolbar.cpp`、`include/gmp/StageLeftToolbar.h`、`src/VtkViewer.cpp`、`include/gmp/VtkViewer.h`、`src/MainWindow.cpp::run_screenshot_tour`。
- **修复方案**：
  1. 使用项目自绘 20 px 图标替换全部字母占位，保留 Tooltip、可访问名称、互斥选中态和 36×34 px 热区。
  2. `.msh` 的显示方式改为“表面 → 线框 → 表面 + 边线”循环；`.e` 继续复用结果表示模式。
  3. 舞台选择在 `.msh` 映射为网格 Pick、在 `.e` 映射为 Probe；剖切在非 `.msh` 模式明确提示不可用；所有舞台命令增加状态栏/消息区反馈。
  4. 拾取/草图接管左键时中止事件继续传播；普通相机交互只由 VTK 交互样式处理，不再手工重复调用。
  5. GUI 巡览增加真实页签点击、重复点击当前 Part、Parts 根/子节点选择及 12 个舞台按钮点击。
- **验证结果**：构建通过；CTest `1/1` 通过；带 `out/box.msh` 和空舞台两种条件下的真实点击 GUI 巡览均完成 34 步，包含 Part 页签首次/重复点击、Parts 根/子节点与全部通用舞台按钮，进程正常退出；空舞台命令均给出原因提示。2026-09-02 用户确认人工回归通过。
- **状态**：已修复

## 2026-09-02-002 新建或双击草图未显示编辑工作窗

- **影响接口/模块**：Phase 1 兼容工作窗、Sketch 模块、模型树 Sketches 节点与 2D 草图编辑会话。
- **现象**：点击“新建草图”后树中能生成节点、舞台左侧出现草图工具，但没有可见的草图编辑工作窗；双击模型树中的草图也不显示编辑窗。
- **复现步骤**：
  1. 进入“草图”模块并点击“新建草图”。
  2. 关闭可能存在的 Module Workspace，在模型树中双击任一 Sketches 子节点。
  3. 观察舞台与浮动工作窗状态。
- **根因**：
  1. `open_sketch_editor` 仅建立 `SketchDocument` 会话、切换 VTK 2D 模式和调用 `SketchPanel::set_editing(true)`，未显示承载 `SketchPanel` 的 `module_work_window_`。
  2. 新建入口只调用 `add_child_item`，依赖树选择信号间接进入编辑，没有统一执行“创建并打开”。
  3. 模型树未连接 `itemDoubleClicked`；旧逻辑反而在单击 Sketches 子节点时自动进入编辑，与 REQ-005 的“单击选择、双击编辑”不一致。
  4. SketchPanel 内部列表的双击处理错误跳转到 Property 模块，没有调用草图编辑入口。
  5. 编辑态与管理态同时占用同一纵向布局，编辑控件位于列表下方，即使工作窗显示也不易被发现。
- **关联逻辑/路径**：`src/MainWindow.cpp` 中草图会话、新建入口、模型树信号和 GUI 巡览；`src/SketchPanel.cpp`、`include/gmp/SketchPanel.h`。
- **修复方案**：
  1. 建立统一 `create_and_open_sketch` 路径，新建后立即选中节点并打开编辑会话。
  2. `open_sketch_editor` 显示、置前并激活 Module Workspace，标题关联当前草图；完成编辑或关闭窗口时保存、退出 2D 并隐藏工作窗。
  3. 树单击只同步选择和 Sketch 上下文；树双击、SketchPanel 列表双击和“打开编辑”统一调用 `open_sketch_editor`。
  4. SketchPanel 分为管理态与编辑态；编辑时隐藏管理列表，从工作窗顶部直接显示绘图、约束、尺寸、撤销/重做和完成编辑控件。
  5. GUI 巡览增加新建打开、关闭、树双击重开和再次关闭四条真实交互路径。
- **验证结果**：构建通过；CTest `1/1` 通过；38 步真实点击 GUI 巡览正常退出。截图确认“新建草图”和“树双击草图”均显示编辑工作窗，且编辑控件从窗口顶部直接可见。2026-09-02 用户确认人工回归通过。
- **状态**：已修复

## 2026-09-02-003 完成草图编辑后舞台内容消失且部件编辑窗未打开

- **影响接口/模块**：Phase 1 兼容工作窗、Sketch 2D 会话、Part 模块、模型树 Sketches/Parts 子节点。
- **现象**：草图编辑时图元可见，点击“完成编辑”后树仍选中当前草图，但舞台内容被清空；点击“新建部件”或双击 Parts 子节点时没有稳定显示部件编辑工作窗。
- **复现步骤**：
  1. 新建或双击草图，绘制圆或直线后点击“完成编辑”。
  2. 观察当前草图仍被选中，但舞台图元消失。
  3. 新建部件并选择来源草图，或双击模型树中的 Parts 子节点。
  4. 观察 Module Workspace 是否显示并置前 Part 页面。
- **根因**：
  1. `close_sketch_editor` 保存后直接调用 `set_sketch_document(nullptr)`，该 API 会隐藏所有草图 actor 并恢复 3D 场景；树选择未变化，因此出现“选中对象与舞台展示不一致”。
  2. Viewer 只有“可编辑草图文档”与“无草图”两态，没有完成编辑后的只读预览态；若直接保留原指针又会在释放编辑文档后产生悬空指针风险。
  3. Part 的顶部新建入口仍走通用 `add_item_under_root`，模块页新建虽选择了来源草图，但创建后没有显示工作窗；Parts 树双击也没有专用处理，只存在默认的属性跳转语义。
  4. 部件名按固定/计数后缀生成时未检查现有同名节点，历史入口混用后可能生成重复名称。
- **关联逻辑/路径**：`include/gmp/VtkViewer.h`、`src/VtkViewer.cpp` 草图会话；`src/MainWindow.cpp` 草图关闭/选择、部件创建/打开和 GUI 巡览。
- **修复方案**：
  1. Viewer 新增拥有文档副本的只读草图预览态；完成编辑时先复制快照再释放编辑文档，保留 actor 与 2D 取景。
  2. 只读态阻断鼠标绘制、Delete、工具切换、几何约束和尺寸修改；选择另一个草图时切换预览，离开 Sketch 模块时清除预览并恢复原 3D 场景。
  3. 建立统一 `open_part_editor`，新建、Parts 树双击、部件列表双击和“打开所选部件”均切到 Part 页面、同步选择并显示/置前工作窗。
  4. 顶部与模块内“新建部件”统一执行来源草图选择、唯一名称生成、节点创建和立即打开。
  5. GUI 巡览加入实际图元、只读预览状态/实体数量断言、Parts 双击标题断言以及自动确认来源草图的新建部件路径。
- **验证结果**：构建通过；CTest `1/1` 通过；带 `out/box.msh` 的真实点击 GUI 巡览通过新增的草图预览、Parts 树双击和新建部件断言并正常退出。2026-09-02 用户确认 `TEST-P1-L03-07` 人工回归通过。
- **状态**：已修复

## 2026-09-02-004 新建项目图标与网格图标近似且 Display Group 无法同排停靠

- **影响接口/模块**：Phase 1 / L-05；顶部 Project/Mesh 工具组、Display Group、工具栏布局持久化。
- **现象**：18 px 下“新建项目”的方框加号与“生成网格”四宫格视觉近似；Display Group 从浮动状态拖回顶部时只能占据独立第二行，无法与 Project、Edit、Model、Mesh、Job 同排。
- **复现步骤**：
  1. 启动应用，观察顶部第一个“新建项目”图标并与“生成网格”比较。
  2. 执行“恢复默认工具布局”，把右上角 Display Group 拖到顶部现有工具栏行。
  3. 观察 Display Group 被放入独立停靠行。
- **根因**：
  1. `NewFile` 使用“正方形 + 十字”图形，低分辨率栅格化后与 Mesh 的网格线构图接近。
  2. Display Group 使用 `QDockWidget`，其停靠区域与顶部五个 `QToolBar` 的工具栏行不是同一套布局容器；即使都位于顶部区域，也只能形成独立的 dock 行。
- **关联逻辑/路径**：`src/MainWindow.cpp::MakeIcon`、`src/MainWindow.cpp::build_toolbar`、`src/MainWindow.cpp::reset_tool_group_layout`、`src/MainWindow.cpp::run_screenshot_tour`、`include/gmp/MainWindow.h`。
- **修复方案**：
  1. 将新建项目图标重绘为带折角的文档轮廓，并使用蓝色加号强化“新建”语义；网格图标保持四宫格。
  2. 将 Display Group 改为标准 `QToolBar`，与其他五组共用 `QMainWindow` 工具栏布局；默认状态通过顶层 Tool 形态悬浮在舞台右上，同时保持工具栏注册以支持状态序列化和拖回同一行。
  3. 默认恢复时显式规范工具栏父级窗口标志；因持久化对象类型变化，将布局配置升级到 `ui/layout/v3` 并忽略旧 v2 状态；自动巡览新增“Display Group 插入顶部既有工具栏行”的位置断言。
- **验证结果**：构建通过；48 步真实点击 GUI 巡览通过，默认浮动位置、布局 round-trip、Display Group 第一行同排停靠断言均通过；截图确认两个图标已形成明显不同轮廓。2026-09-02 用户确认 `TEST-P1-L05-06` 人工回归通过。
- **状态**：已修复

## 2026-09-02-005 浮动弹窗沿用旧右栏布局并出现双竖向滚动条

- **影响接口/模块**：Phase 2 / I-01、I-01A、I-04；Module Workspace、FloatingPropertyForm、PartFeaturePanel、Mesh/Gmsh、Job/MOOSE、Visualization、Results 工作窗。
- **现象**：材料等模块弹窗仍按原右侧属性栏的窄列方式纵向堆叠；外层工作窗和页面内容各有一条竖向滚动条，主要按钮、列表和底部操作不能在默认尺寸内同时看到；Job 的路径、网格、运行、输入、日志全部堆在一列，窗口会被内容尺寸撑高。
- **复现步骤**：
  1. 打开 Material Workspace，观察右边缘存在内外两条竖向滚动条。
  2. 依次打开 Part、Section、Step、Load，观察主操作纵向排列、内容留白与底部按钮裁切。
  3. 打开 Job Workspace，观察作业列表、MOOSE 设置和日志纵向堆叠造成窗口过高。
- **根因**：
  1. `module_work_window_` 外层保留 `QScrollArea`，`make_module_page` 又为每页创建 `QScrollArea`，形成嵌套滚动。
  2. `PropertyEditor` 和 `MoosePanel` 为适配旧右栏强制使用 `QFormLayout::WrapAllRows`，迁入宽浮窗后没有重新设计标签—字段关系。
  3. Part 四类特征及 Job/MOOSE 多类任务都采用单列连续堆叠，没有利用页签表达互斥任务上下文。
  4. 历史几何配置会恢复旧窄窗或超高窗尺寸，覆盖新的合理默认值。
- **关联逻辑/路径**：`src/MainWindow.cpp`、`src/FloatingPropertyForm.cpp`、`src/PropertyEditor.cpp`、`src/PartFeaturePanel.cpp`、`src/MoosePanel.cpp`、`src/L10n.cpp`。
- **修复方案**：
  1. 移除 Module Workspace 的外层与模块页内部滚动容器，主操作横排，对象列表自适应，对象操作固定在底部；自动断言禁止嵌套 `QScrollArea`。
  2. PropertyEditor 拆为“常规/参数/校验/预览”四页签；PartFeaturePanel 拆为“拉伸/旋转/放样/扫掠”四页签；宽窗表单恢复标签与字段同排。
  3. Job 拆为“作业列表/MOOSE 设置”，MOOSE 设置再按“算例设置/执行配置/输入文件/运行日志”分区，运行操作固定在底部。
  4. Mesh/Gmsh 拆为“模型/几何/分组与网格场/网格/日志”，长表单仅在所属任务页保留单层按需滚动。
  5. Visualization、Results 去除无必要的外层滚动；升级相关窗口几何配置键，首次打开采用新默认尺寸并限制在可用屏幕内。
- **验证结果**：构建通过；CTest `1/1` 通过；标准 GUI 截图巡览 19 步通过；带 `out/box.msh` 的真实点击 GUI 巡览 52 步全部通过。截图确认 Material、Part、Property、Mesh、Job、Visualization、Results 默认状态无双层竖向滚动，主要交互均可通过当前页或页签访问。2026-09-02 用户确认人工验收通过。
- **状态**：已修复

## 2026-09-03-001 模型树过滤清除按钮过大且对象/状态列宽不可调

- **影响接口/模块**：Phase 2 / I-02；Model/Results 左侧导航、名称过滤、树表头。
- **现象**：名称过滤框右侧使用独立的 30×30 清除按钮，平台标准重置图标显示为视觉重量过大的扫帚；Model/Results 树的对象列使用 Stretch、状态列使用 ResizeToContents，用户无法拖动表头分隔线调整列宽。
- **复现步骤**：
  1. 打开 Model 或 Results 左侧导航，观察过滤框右侧清除按钮。
  2. 尝试拖动“对象/状态”表头之间的分隔线。
- **根因**：过滤清除入口复用了 `QStyle::SP_DialogResetButton` 并放置在独立固定尺寸按钮中；两列显式设置为自动布局模式，覆盖了 `QHeaderView` 的交互式调整能力。
- **关联逻辑/路径**：`src/MainWindow.cpp` 左侧导航构造与 `run_screenshot_tour()`；`doc/UI重构Phase2人工验收清单.md`。
- **修复方案**：移除独立清除按钮，统一使用 `QLineEdit` 内嵌原生清除按钮；Model/Results 表头两列改为 `QHeaderView::Interactive`，设置合理初始宽度和最小宽度，禁止列顺序移动但允许拖动列宽。
- **验证结果**：构建通过；CTest `1/1` 通过；56 步真实点击 GUI 巡览全部通过。自动断言确认 Model/Results 四个表头分区均为 Interactive，输入过滤文本后内嵌清除按钮不高于输入框；巡览截图确认原独立大号按钮已移除、两列表头边界清晰。
- **状态**：已修复；2026-09-03 用户随 I-02 人工验收确认通过

## 2026-09-03-002 Part 特征操作重复创建相同部件

- **影响接口/模块**：Phase 2 / I-03；Part 工作窗、Parts/Features 模型树、拉伸/旋转/放样/扫掠结果写回。
- **现象**：先创建草图，再新建 `part_1`，选中 `part_1` 执行拉伸后又出现几何相同的 `part_2`；重复执行特征会继续增加 Part。
- **根因**：新建部件流程已经预先创建目标 Part，但统一特征结果处理仍沿用旧实现——每次特征成功后无条件创建新的 Part。特征请求未携带当前 Part，结果处理也没有目标校验，造成“显式创建 Part”和“特征自动产出 Part”两套生命周期叠加。
- **关联逻辑/路径**：`src/MainWindow.cpp` 的 Part 选择同步、特征信号接线、`active_part_item()` 与 `attach_feature_to_part()`；`run_screenshot_tour()`。
- **修复方案**：特征执行前解析并校验当前 Part；成功后只新增 Feature 历史，将 `feature`、`sketch`、`gmsh_volume_tag`、BREP 路径写回该 Part，并在 Feature 中保存 `part` 反向关联。操作完成后恢复当前 Part 选择；未选中 Part 时阻止操作并提示先选择或新建。Part 列表单击同步为全局活动对象。
- **验证结果**：自动巡览新增断言，要求特征写回前后 Parts 数量不变、Features 数量增加 1、Part/Feature 双向关联正确且当前对象保持原 Part；人工复验见 `TEST-P2-I03-06`。
- **状态**：已修复；2026-09-06 用户随 I-03 人工验收确认通过

## 2026-09-03-003 后台执行 GUI 巡览在表单焦点断言处异常退出

- **影响接口/模块**：Phase 2 自动验收；macOS 后台 GUI 巡览、I-01 表单校验断言。
- **现象**：巡览执行到 `i01_property_form_commit` 后产生 `EXC_CRASH (SIGABRT)`；系统报告显示 `abort() called`，调用栈落在 `MainWindow::run_screenshot_tour()`。
- **根因**：应用由 Codex 以 Background 角色启动时，macOS 不保证窗口取得系统输入焦点；巡览跨步骤使用 `QLineEdit::hasFocus()`，将操作系统焦点限制误判为表单逻辑失败。失败路径调用 `qFatal()`，Qt 按设计执行 `abort()`，因此被 macOS 记录成应用崩溃。
- **关联逻辑/路径**：`src/MainWindow.cpp::run_screenshot_tour()` 的 I-01 校验和巡览调度器。
- **修复方案**：后台模式验证错误字段可见且可编辑，只有应用处于前台时才断言系统焦点；巡览断言统一抛出异常并由调度器捕获，记录失败步骤后以退出码 2 正常结束，避免生成崩溃报告；增加 `GMP_TOUR_STEP_FILTER` 以便单独复验指定步骤。
- **验证结果**：原崩溃点 `i01_property_form_commit` 已通过；带 `out/box.msh` 的完整 61 步真实点击巡览全部通过并正常退出，新增 Part 写回回归也通过。
- **状态**：已修复

## 2026-09-03-004 草图二维视口工具及撤销重做快捷键失效

- **影响接口/模块**：Phase 2 / I-03；Sketch 编辑/预览、舞台左侧通用工具栏、Tools 菜单与共享编辑工具栏。
- **现象**：草图二维舞台中“选择”提示只能拾取 `.msh/.e`，平移/缩放按钮虽然显示模式已切换但拖动无效；焦点进入浮动 Sketch Editor 后，平台标准撤销/重做快捷键不响应。
- **根因**：二维模式使用 `vtkInteractorStyleImage`，但舞台交互模式只写入 3D `StageInteractorStyle`；同时高优先级草图左键回调无条件终止事件，二维相机收不到拖动。通用拾取、清除和取景也只实现 Mesh/Exodus 分支。撤销/重做 QAction 使用默认 `WindowShortcut`，浮动编辑窗是独立顶层窗口，获得焦点后不属于 MainWindow 的窗口快捷键范围。
- **关联逻辑/路径**：`src/VtkViewer.cpp` 的二维交互样式、草图输入回调及舞台命令；`src/MainWindow.cpp` 的 QAction、舞台接线和 GUI 巡览。
- **修复方案**：新增二维草图交互样式和独立导航状态；导航状态下放行 VTK 事件，选择/绘制状态下仍由草图回调接管。通用选择、清除和适配窗口增加 Sketch 分支，只读预览允许选择但拒绝修改。整体视图平移最终统一为中键，左键四向箭头的图元移动语义见后续缺陷 2026-09-03-005。撤销/重做改为 `Qt::ApplicationShortcut`，编辑会话外仍按原规则禁用。
- **验证结果**：构建通过；CTest `1/1` 通过；完整 62 步真实点击 GUI 巡览通过并正常退出。新增回归实际执行草图 YAML 快照撤销/重做，并断言二维平移与选择模式切换、应用级快捷键作用域及草图适配窗口。
- **状态**：已修复；2026-09-06 `TEST-P2-I03-07` 人工验收通过

## 2026-09-03-005 草图“移动图元”误实现为视图平移且与选择同时高亮

- **影响接口/模块**：Phase 2 / I-03；Sketch 编辑、舞台左侧工具栏、Sketch Editor 工具区、草图撤销栈。
- **现象**：四向箭头按钮只能拖动整个二维视图，无法命中并移动矩形内部的圆等图元；同时四向箭头、通用拾取和草图选择可一起高亮，无法判断当前左键究竟执行移动还是选择。
- **根因**：四向箭头沿用了 3D 场景的相机 Pan 语义，草图模型没有 Move 工具和按下/移动/释放事务；通用交互、拾取、草图工具属于三个独立按钮组，只在各组内部互斥，未定义 Sketch 上下文中的跨组主操作状态。
- **关联逻辑/路径**：`src/StageLeftToolbar.cpp` 的 Sketch 上下文路由；`src/VtkViewer.cpp` 的草图按下/移动/释放事件；`src/SketchDocument.cpp` 的图元平移；`src/SketchPanel.cpp` 的工具同步。
- **修复方案**：新增 `SketchToolMove`。移动工具按下时使用草图命中测试选择目标，拖动期间按初始快照平移选中集合，释放后只提交一次修改并进入撤销栈；有约束时在释放后求解。Sketch 上下文将四向箭头定义为“移动图形”，整体视图平移保留给中键。隐藏重复的通用拾取和无效旋转入口，并让移动、缩放及草图工具跨组互斥，同时同步浮动 Sketch Editor。
- **验证结果**：构建通过；CTest `1/1` 通过，新增直线、圆、圆弧平移契约测试；完整 62 步真实点击 GUI 巡览通过，自动断言“移动图形/缩放/选择”互斥、通用拾取在 Sketch 中隐藏且工具语义正确。
- **状态**：已修复；2026-09-06 `TEST-P2-I03-08` 人工验收通过

## 2026-09-03-006 矩形缺少逻辑图形身份，只能逐边选择和移动

- **影响接口/模块**：Phase 2 / I-03；SketchDocument 数据模型、矩形绘制、选择/移动命令、草图 YAML 兼容。
- **现象**：圆可以作为整体选择和移动，但矩形只能命中一条边；用户无法在“整个矩形”“矩形单边”“多个完整图形”“多条边”之间表达选择范围。
- **根因**：底层只支持 Line/Circle/Arc。Rectangle 绘制被展开为 4 条 Line 和 Coincident 约束，没有持久化的逻辑图形标识；命中测试只返回最近的底层 entity id。
- **关联逻辑/路径**：`include/gmp/SketchDocument.h`、`src/SketchDocument.cpp` 的实体/YAML 模型；`src/VtkViewer.cpp` 的草图选择和移动事务。
- **修复方案**：为草图实体增加 `shape_id`，同一矩形的四边共享一个逻辑图形 id，独立图元各自成组。默认点击把命中扩展为整个逻辑图形；`Option/Alt` 保持底层子图元选择；`Shift` 对当前粒度追加/移除选择，因此支持多完整图形和多边混合选择。YAML 保存 `shape_id`；旧数据缺少该字段时根据 Coincident 连接分量恢复复合图形。
- **验证结果**：构建通过；CTest `1/1` 通过。新增契约覆盖矩形四边同组、整形平移、YAML 往返及旧矩形分组恢复；既有线、圆、圆弧平移回归继续通过。
- **状态**：已修复；2026-09-06 `TEST-P2-I03-09` 人工验收通过

## 2026-09-03-007 周期面网格失败后重复回退导致交叉数爆炸

- **影响接口/模块**：Part 特征自动预览、OCC → Gmsh 回注、自动生成 `.msh`。
- **现象**：圆柱面剖分先报 `Impossible to mesh periodic surface 1`，随后又开始一次 2D 剖分；第二次剖分报告 `19025 intersections in the 1D mesh`，持续输出 `Splitting those edges and trying again`，只能通过 `Ctrl+C` 中止进程。
- **复现步骤**：创建能触发 Gmsh 周期圆柱面剖分错误的 Part 特征并执行自动网格；观察首次 3D 生成失败后的 2D 回退。
- **根因**：`mesh_current_model()` 固定先执行 `generate(3)`，捕获异常后直接在同一模型上执行 `generate(2)`。Gmsh 的失败调用已留下部分 1D/2D 网格，未清理的残留被第二次调用重复参与剖分；而周期面错误本就发生在 3D 流程包含的低维阶段，降为 2D 只会重复同一失败路径。
- **关联逻辑/路径**：`src/OccBridge.cpp::mesh_current_model()`、`include/gmp/OccBridge.h`。
- **修复方案**：生成前统一清除旧网格；根据当前模型是否存在 Volume 选择 3D 或 2D，面/壳模型不再先尝试 3D；任何生成异常后先清除部分网格；周期面、1D/2D 网格错误快速失败并跳过重复 2D 回退，仅对纯 3D 算法错误保留清理后的 2D 表面网格回退；错误信息明确说明已清理残留并跳过重复回退。
- **验证结果**：全量应用构建通过；CTest `1/1` 通过；加载 `out/box.msh` 的完整 62 步真实点击 GUI 巡览正常退出。使用项目真实的 `SketchDocument → OCC 拉伸 → Gmsh` 链路完成圆柱回归，成功生成 1 个 Volume、167 个节点、822 个单元并写出 `.msh`。代码路径确认附件中的周期面错误会在首次异常后清理并返回，不再进入产生 19025 个交叉的第二轮 2D 剖分；2026-09-06 用户随 I-03 人工验收确认问题不再复现。
- **状态**：已修复；2026-09-06 用户随 I-03 人工验收确认通过

## 2026-09-03-008 草图工具切换被拾取状态回写覆盖

- **影响接口/模块**：Phase 2 / I-03；草图只读预览、舞台左侧工具栏、顶部 Display Group、Sketch Editor。
- **现象**：点击“移动图形”等其他按钮后仍然只有点选生效；顶部拾取、左侧选择及其他工具的高亮与实际交互状态不一致。
- **根因**：只读预览拒绝修改工具后直接回写 Select，但工具按钮仍保持可点击，形成“可切换但立即复位”的假象；同时三个入口分别手工同步，`stage_picking_changed` 被同时当作 3D 拾取开关和草图工具状态使用。悬浮工具组的鼠标释放保护还会重新启用拾取，再次强制覆盖刚切换的草图工具。
- **关联逻辑/路径**：`src/VtkViewer.cpp` 的草图工具/拾取状态；`src/MainWindow.cpp` 的工具接线、预览转编辑和悬浮工具事件过滤；`src/MainWindow.cpp::run_screenshot_tour()`。
- **修复方案**：新增 `sketch_tool_changed` 作为草图工具唯一状态回写，由视口统一同步顶部拾取、舞台左栏和 Sketch Editor；预览态点击移动、绘制、删除等修改工具时自动进入当前草图编辑会话并保留目标工具；草图会话绕过只适用于 3D 拾取的悬浮工具拖动保护；Select 在草图中按互斥工具处理，不允许顶部 Pick 显示关闭而实际仍在选择。
- **验证结果**：构建通过；CTest `1/1` 通过；完整 64 步真实点击 GUI 巡览通过。新增步骤验证预览态 Move 自动进入编辑，以及顶部 Pick、左侧 Select/Move、Sketch Editor Select/Move 往返切换时的实际工具和高亮完全一致。
- **状态**：已修复；2026-09-06 `TEST-P2-I03-10` 人工验收通过

## 2026-09-04-009 草图绘制工具无持续高亮且编辑窗被隐藏页面撑大

- **影响接口/模块**：Phase 2 / I-03；Sketch Editor、Module Workspace 堆叠容器、主题样式及窗口几何持久化。
- **现象**：直线、圆、圆弧、矩形等工具实际仍可连续绘制，但按钮没有持续高亮，无法直接判断当前工具；Sketch Editor 内容较少却沿用高大的通用工作窗，“完成编辑”横跨整窗且下方存在大量空白。
- **根因**：草图工具使用可选中的 `QPushButton`，全局主题却只定义了 `QToolButton:checked`，导致逻辑状态存在但没有视觉状态；`QStackedWidget` 默认使用所有页面的最大尺寸提示，隐藏的 Mesh/Part 等复杂页面把当前 Sketch 页的最小高度撑到约 706；同时 Sketch 与其他模块共用一份窗口几何。
- **关联逻辑/路径**：`src/SketchPanel.cpp` 的编辑器布局和工具属性；`src/MainWindow.cpp::apply_theme()`、`CurrentPageStackedWidget`、`apply_module_workspace_profile()` 及 GUI 巡览。
- **修复方案**：为草图工具添加蓝底白字的持久 checked 样式和独立“当前工具”状态条；编辑区改为工具与约束/尺寸双栏，底部撤销、重做、完成编辑合并为操作行，完成按钮限制合理宽度；堆叠容器只按当前页计算尺寸，Sketch 使用独立 `680×350` 默认、`640×320` 最小 profile 和单独的版本化几何键，切换时保存/恢复各自尺寸并执行屏幕边界校正。
- **验证结果**：构建通过；CTest `1/1` 通过；完整 64 步真实点击 GUI 巡览通过。新增断言确认矩形工具持续 checked、当前工具状态同步、Sketch profile 为紧凑尺寸且完成按钮最大宽度不超过 160；巡览截图确认按钮高亮和双栏紧凑布局生效。
- **状态**：已修复；2026-09-06 `TEST-P2-I03-11` 人工验收通过

## 2026-09-04-010 删除草图和部件后 3D 舞台仍残留旧模型

- **影响接口/模块**：Phase 2 / I-03；模型树删除、Sketch/Part/Feature 生命周期、VTK 网格舞台。
- **现象**：草图拉伸生成 Part 后，依次删除草图和 Part，模型树计数已经归零，但右侧 3D 舞台仍显示此前生成的网格。
- **根因**：模型树删除仅移除 `QTreeWidgetItem`，没有失效依赖对象或通知视口；`VtkViewer::set_mesh_file("")` 和 `set_exodus_file("")` 对空路径直接返回，也没有统一清理 Actor、管线和文件监听；特征生成的 MESH 路径未写入 Part/Feature 参数，删除时无法判断当前舞台数据的所有权。
- **关联逻辑/路径**：`src/MainWindow.cpp::remove_item()`、Part Feature 结果写回；`include/gmp/VtkViewer.h`、`src/VtkViewer.cpp::clear_stage_data()`；GUI 巡览。
- **修复方案**：特征成功后将 MESH 路径同时写入目标 Part 和 Feature；删除被引用的 Sketch、Part、Feature、Mesh 或当前 Results 时统一清除视口文件、监听、数据管线、主/节点/轮廓/选择 Actor、标尺和交互状态；删除 Part 级联删除以其为 `part` 的 Feature，删除 Feature 同步移除 Part 上的几何结果引用。保留磁盘生成文件，避免不可恢复的数据删除。
- **验证结果**：构建通过；CTest `1/1` 通过；完整 65 步真实点击 GUI 巡览通过。新增步骤真实加载 `out/box.msh`，分别验证删除依赖草图和删除 Part 后 `has_stage_data=false`、所有舞台 Actor 不可见、所属 Feature 数量减少且不留孤立项，并验证清空后可再次加载网格。
- **状态**：已修复；2026-09-06 `TEST-P2-I03-12` 人工验收通过

## 2026-09-04-011 多轮廓拉伸在 Sketch/Part 往返后只剩一个实体

- **影响接口/模块**：Phase 2 / I-03；Sketch 多闭合轮廓、OCC 拉伸结果、Part/Feature 参数、模型树到舞台联动。
- **现象**：同一草图中的两个相离闭合图形沿 Z 轴拉伸后，首次显示两个 3D 实体；切换到 Sketch 再返回 Part，只剩其中一个实体可见。状态栏仅报告 `volume 1`。
- **根因**：OCC 已正确生成 Compound，Gmsh 也正确导入并划分了两个 Volume；但 `FeatureResult`、Part 和 Feature 只保存首个 `gmsh_volume_tag`。模型树重新选中 Part/Feature 时按该单一标签调用实体过滤，因而把同一 Part 的其余 Volume 隐藏。几何与网格没有丢失，错误发生在恢复显示阶段。
- **关联逻辑/路径**：`include/gmp/OccBridge.h`、`src/OccBridge.cpp::finalize_feature()`；`src/MainWindow.cpp` 特征写回、模型树选择和网格反向定位；`src/VtkViewer.cpp::visible_mesh_entity_count()`；GUI 巡览。
- **修复方案**：特征结果收集全部 Volume 标签并以 `gmsh_volume_tags` 写入 Part/Feature，同时保留首标签兼容旧项目；选择带自有 MESH 的 Part/Feature 时重新载入对应文件并取消单实体过滤，恢复该 Part 的完整网格；无自有 MESH 的多标签旧数据也不再错误收窄到首标签；舞台反向拾取可用标签集合中的任一成员定位所属 Part/Feature。
- **验证结果**：构建通过；CTest `1/1` 通过；完整 66 步真实点击 GUI 巡览通过。新增回归真实构造两个相离圆并沿 Z 轴拉伸，Gmsh 日志确认 `2 volumes with 2 connected components`、Volume 1/2 均完成网格；自动断言初次显示和 Sketch→Part 往返后可见 Volume 数都为 2，Part 保存的 Volume 标签数也为 2。
- **状态**：已修复；2026-09-06 `TEST-P2-I03-13` 人工验收通过

## 2026-09-04-012 模型树允许创建同名对象且命名冲突无法原位重试

- **影响接口/模块**：Phase 2 / I-03；模型树全部根节点、模块对象列表、Sketch/Part 专用入口、复制与重命名。
- **现象**：Parts 已存在 `part_1` 时，“Add Parts”仍默认 `part_1`；直接确认后可以再次创建同名节点。其他根节点的通用添加、重命名和连续复制也缺少一致的名称冲突保护。
- **根因**：通用新增直接使用一次性的 `QInputDialog::getText()`，确认后立即构造树节点；底层 `add_child_item()` 不检查同级名称；重命名走无校验的内联编辑；复制固定追加 `_copy`。各专用入口分别生成名称，没有共享唯一性规则。
- **关联逻辑/路径**：`include/gmp/MainWindow.h`；`src/MainWindow.cpp::child_name_exists()`、`unique_child_name()`、`prompt_unique_child_name()`、`add_child_item()`、`add_item_under_root()`、`rename_item()`、`duplicate_item()`；GUI 巡览。
- **修复方案**：建立同一根节点内的统一名称检查与唯一名称生成；用户新增/重命名改用可持续校验的命名对话框，空名称或重名时在窗内显示错误、保持焦点和弹窗，不提交节点；所有模型树和模块列表的重命名入口统一接入；复制与无弹窗的内部创建自动追加唯一后缀；底层插入执行最终唯一化兜底。
- **验证结果**：构建通过；CTest `1/1` 通过；完整 67 步真实点击 GUI 巡览通过。新增步骤在 Parts 已有 `part_1` 时再次提交同名，自动断言节点数不变、错误可见且对话框未关闭；改为唯一名称后才新增。随后连续以相同程序名插入两次，断言最终节点名称不同。
- **状态**：已修复；2026-09-06 `TEST-P2-I03-14` 人工验收通过

## 2026-09-06-013 打开含脚本错误的 .geo 后导入静默失败且整个会话无法再导入几何

- **影响接口/模块**：Phase 2 / I-04（TEST-P2-I04-02 人工预验收发现）；Mesh 工作窗 Gmsh 面板“打开几何”、几何 → 网格 → 中央舞台联动。
- **现象**：在 Mesh 工作窗选择一个 `.geo` 文件后，中央舞台仍显示旧模型，面板摘要为 0 实体，没有任何错误提示；此后即使选择完全合法的 `.geo` 文件也同样加载不出来。用户在导入失败后执行网格生成，还会把空网格（0 节点 0 单元）覆盖写到输出路径（本地 `out/box.msh` 测试夹具即被这样破坏）。
- **复现步骤**：
  1. 启动应用，打开 Mesh 工作窗，选择含有非法语句的 `.geo`（如在 OCC 内核下混入 `Wire`/`Split Curve` 等内置几何命令的 `demo.geo`）。
  2. 观察模型页签摘要、实体列表和中央舞台。
  3. 再次打开一个合法 `.geo`，观察依然无法加载。
- **根因**：
  1. Gmsh API 初始化默认 `General.AbortOnError=2`，`.geo` 脚本错误使 `gmsh::open` 抛异常；异常路径不释放 gmsh 内部的 busy 标志，之后所有 `gmsh::open` 都被 “I'm busy! Ask me that later...” 静默吞掉，会话级损坏。
  2. `import_geometry` 捕获异常后只把错误写进“运行日志”页签，没有可见反馈，也不刷新面板状态，旧路径与旧摘要造成“已加载”的假象；`model_loaded_` 保持旧值，后续网格生成在空模型上写出空网格。
- **关联逻辑/路径**：`src/GmshPanel.cpp::import_geometry()`、`on_open_geometry()`；`include/gmp/GmshPanel.h`；GUI 巡览。
- **修复方案**：
  1. 导入期间临时把 `General.AbortOnError` 改为 0（用前值恢复）：脚本错误只进 logger，busy 标志不再卡死；导入后扫描 logger 的 `Error` 行，有错误或零实体统一按失败处理并丢弃残缺模型，恢复选项原值。
  2. 新增 `last_import_error()`；`on_open_geometry` 失败时弹出可见告警并附错误详情；失败路径如实清空路径框与实体摘要，`model_loaded_=false`，阻断空网格生成级联。
  3. 面板如实反映失败后的空状态，不再保留“加载成功”的假象。
- **验证结果**：构建通过；CTest `1/1` 通过；71 步真实点击 GUI 巡览通过。新增 `i04_geo_import_feedback` 步骤：先导入含非法语句的 `.geo` 断言返回失败且错误信息非空，再导入合法 `.geo` 断言成功（直接覆盖 busy 卡死回归）。独立复现程序确认 `AbortOnError=0` 后坏文件报错、会话保持可用。被覆盖的 `out/box.msh` 已按应用样例盒参数（含 `solid`/`boundary` 物理组）重新生成。
- **状态**：已修复；待 TEST-P2-I04-01/02 人工复验

## 2026-09-06-014 带孔圆环 .geo 网格生成报 Invalid boundary mesh（几何建模错误 + gmsh/OCC 限制）

- **影响接口/模块**：Mesh 工作窗几何导入与网格生成（TEST-P2-I04-01/02 人工验收中发现）。
- **现象**：打开 `demo_fixed.geo`（旧版：xy 平面矩形旋转 + 双圆柱布尔开孔）后点击“生成网格”，报错 `Invalid boundary mesh (overlapping facets) on surface 2 surface 5`，任何网格尺寸/阶数/算法组合都失败。
- **根因（两层，均非应用逻辑错误）**：
  1. **脚本建模错误**：Gmsh 的 `Rectangle()` 只能建在 xy 平面，脚本注释声称矩形在 xz 平面，实际旋转后得到 z 向零厚度的退化环片（体积质量为 0），其面网格必然出现重叠面片。用独立程序打印实体包围盒与体积质量确认（z 区间 [-0.5,-0.5]，mass=0）。
  2. **gmsh/OCC 限制**：即使建成正确的圆环（`Torus` 原语或正确截面旋转），圆环与圆柱的布尔交线是四次空间曲线，本机 gmsh 4.15.2/OCC 无法使其形成闭合网格环（报 `The 1D mesh seems not to be forming a closed loop`）；gmsh CLI 独立复现同样失败。纯圆环、盒体-圆柱布尔、圆环-平面盒切割（交线为平面圆）均验证可正常网格化（1~4 阶）。
  3. 排障过程中还发现 gmsh `AbortOnError=0` 会把此类 3D 失败降级为日志错误并只产出面网格（`No elements in volume 1`），会掩盖真实失败——应用内生成路径保持默认 `AbortOnError=2`，能如实抛出。
- **关联逻辑/路径**：`src/GmshPanel.cpp::import_geometry()`、`on_generate()`；用户文件 `~/Downloads/demo.geo`、`~/Downloads/demo_fixed.geo`。
- **修复方案**：
  1. `import_geometry` 新增退化实体检查：导入后对每个 Volume 计算 OCC 质量，零体积直接失败并给出可读原因（“Degenerate solid with zero volume detected…”），把此类错误从生成阶段提前到导入阶段。
  2. `~/Downloads/demo_fixed.geo` 重写为已验证版本：先在 xy 平面建矩形再绕 x 轴旋转 90° 得到真正的 xz 截面，旋转成环后用方盒切 90° 扇区（交线为平面圆），保留“旋转成形 + 布尔”的演示意图；文件头部写入排坑记录。
  3. 巡览 `i04_geo_import_feedback` 新增退化几何夹具（xy 矩形直接旋转）断言导入失败且原因含 “Degenerate”。
- **验证结果**：构建通过；CTest `1/1` 通过；72 步真实点击 GUI 巡览通过。独立程序验证新 `demo_fixed.geo` 在脚本尺寸与 0.1/四阶参数下均干净生成（3495 / 532052 节点，0 错误）；盒体交叉孔版本（`/tmp/box_holes.geo`）同样通过。
- **状态**：已修复；待用户用新 `demo_fixed.geo` 复验

## 2026-09-08-015 手画多线段轮廓内的圆拉伸后不中空

- **影响接口/模块**：草图 → 拉伸成型（`OccBridge::faces_from_sketch` / `sample_loop_polygon`）。
- **现象**：直线连接的凹多边形内画一个圆，拉伸后圆不是孔（日志显示导入 gmsh 为 2 个体：多边形实体 + 圆柱）；而矩形内画圆拉伸后正确成孔（1 个体）。
- **根因**：中空判定依赖“环按奇偶层嵌套”（`faces_from_sketch`：偶数层成实体、奇数层作内孔），嵌套关系用射线法 `point_in_polygon` 判定。但 `sample_loop_polygon` 对线段只取 `p1` 作为采样顶点：手画线段方向任意，顶点顺序被打乱，射线法在乱序多边形上判定失败，内圆深度被误判为 0（偶数层）而单独成体。矩形由工具按一致方向生成 4 条边，“碰巧”一直正确。
- **修复方案**：`sample_loop_polygon` 对线段按环遍历方向做链式端点匹配（链尾接 `p1` 则补 `p2`，接 `p2` 则补 `p1`），保证采样多边形顶点按边界顺序排列；圆弧采样含端点以便混合环链式匹配。
- **验证结果**：新增巡览 `sketch_nested_loop_hole_extrude`：矩形+圆（对照）与方向故意打乱的凹五边形+圆均断言拉伸为 1 个体（带内孔）；82 步全量巡览通过，CTest `1/1` 通过。
- **状态**：已修复

## 2026-09-08-016 结果列表选中变化的 use-after-free 崩溃

- **影响接口/模块**：Results 结果列表（`results_list_`）选中与“以文本打开”。
- **现象**：操作触发 `refresh_results_panel` 重建列表时进程段错误退出（崩溃栈：`currentItemChanged` lambda `MainWindow.cpp:2416` 访问已释放的 `QListWidgetItem`）。
- **根因**：选中处理器先调用 `sync_results_tree_selection(current)`，该路径经模型树选择链可能再次刷新结果列表（`results_list_->clear()` 销毁条目），随后继续 `current->data()` 使用已释放的条目指针。`open_result_as_text` 存在同样的先同步后读取模式。
- **修复方案**：两个处理器都先取数据副本（path/job/text/ext）再调用 `sync_results_tree_selection`；`open_result_in_viewer` 原本就是先读后同步，无需改。
- **验证结果**：82 步全量巡览通过，CTest `1/1` 通过。
- **状态**：已修复

## 2026-09-13-001 Physics 参数有效但节点仍显示“不完整”

- **影响接口/模块**：Phase 4 / `TEST-P4-W03-05`；Physics 属性表单与模型树状态汇总。
- **现象**：Physics 已填写 `action=QuasiStatic`、`block=solid`、`strain=SMALL` 等有效参数并点击“确定”，子节点及 Physics 根节点仍显示“不完整”；表单同时显示只有“类型默认值”一个选项的模板栏。
- **复现步骤**：在 Physics 根节点新增对象，使用自动填充参数并应用 `solid` 体组，点击“确定”，观察模型树状态及模板下拉框。
- **根因**：树状态的通用兜底逻辑把“没有 `type` 和 `path` 字段”视为配置不完整，但 Physics 的对象合同使用 `action`/`strain`，并不包含 `type`；Physics 当前没有第二套可选模板，模板栏只是重复默认参数。
- **关联逻辑/路径**：`src/MainWindow.cpp::refresh_tree_statuses()`、`src/PropertyEditor.cpp::validate_params()`、`src/PropertyEditor.cpp::build_form_for_kind()`、GUI 巡览 `physics_action_contract`。
- **修复方案**：模型树状态改为复用各对象类型自己的参数校验结果，不再统一要求 `type`；Physics 隐藏无实际选择价值的模板栏，新建时仍自动填入 QuasiStatic 默认值；回归增加有效 Physics 保存后必须显示“就绪”及模板控件不存在的断言。
- **验证结果**：构建通过；定向真实点击 GUI 巡览 `physics_action_contract` 通过；Section 状态回归 `section_assignment_contract` 通过；CTest `1/1` 通过。
- **状态**：已修复；待用户复验 `TEST-P4-W03-05`

## 2026-09-14-001 远程作业快照目录污染项目网格路径

- **影响接口/模块**：Phase 4 / `TEST-P4-G0-04`；Job Workspace、Mesh Workspace、模型树 Jobs 持久化与旧项目加载。
- **现象**：选择或恢复远程 Job 后，MOOSE 设置的“网格文件”候选中同时出现真实 `.msh` 和 `case-<时间戳>` 快照目录；错误目录会进一步写入生成报告及 `[Mesh/file] file`，导致输入引用目录而不是网格文件。旧项目的 Mesh 输出还默认指向应用仓库共享的 `out/mes_1.msh`，没有归属到项目工作区；首次路径迁移后，生成报告和结构化输入基线仍可能显示旧路径。
- **根因**：远程作业事件把 `snapshot` 目录存入了 Job 参数的 `mesh` 字段，Job 选中处理又无条件调用 `set_mesh_path()`；旧版 Mesh 新建默认值固定使用进程当前目录的 `out/`，项目加载时没有迁移该历史默认路径。
- **关联逻辑/路径**：`src/MainWindow.cpp` 的远程作业登记、作业选择、作业列表刷新、旧项目加载与 Mesh 默认输出路径；巡览 `remote_job_monitor`、`project_context_isolation_contract`。
- **修复方案**：远程 Job 的 `snapshot` 与本地 Job 的 `mesh` 分字段保存；远程 Job 选择只更新监控状态，不再改写当前项目网格；加载旧项目时自动把远程 Job 的历史 `mesh` 迁移为 `snapshot`。新建 Mesh 默认输出改为项目自有 `.work/case/<项目名>/<网格名>.msh`；旧的仓库 `out/*.msh` 自动复制并改指向该目录，用户明确选择的其他外部 `.msh` 保持不变。迁移同时更新可见输入、结构化输入基线和生成报告；项目加载完成后按当前模型树重建已有生成报告。
- **验证结果**：构建通过；定向真实点击巡览 `remote_job_monitor` 通过，确认远程 Job 登记及选择不改变当前 MOOSE 网格；`project_context_isolation_contract` 通过，确认旧共享网格迁移、文件复制、Mesh/Gmsh/MOOSE 三处路径一致，生成输入/结构化基线/生成报告均清除旧路径，且快照目录不进入候选。
- **状态**：已修复；2026-09-14 用户随 `TEST-P4-G0-04` 人工验收确认通过

## 2026-09-15-001 装配构建成功但中央舞台仍显示单个 Part

- **影响接口/模块**：Phase 5 / G1 / `TASK-P5-01`；Assembly 构建、Mesh Workspace、VTK 中央舞台。
- **现象**：点击“构建装配”后，Mesh Workspace 显示 `assembly: active`、实体摘要包含 `2V`，日志也报告 `Assembly built: 2 visible instance(s)`，但中央舞台仍停留在最后一次拉伸生成的单个 Part 网格。
- **根因**：Assembly 构建只更新 Gmsh/OCC 当前模型，没有为使用 VTK 的中央舞台生成并加载装配预览；补充二维表面预览后，Viewer 又会沿用旧 Part 的实体/单元类型过滤，并按 OCC 中存在的三维几何实体选择维度，导致只有二维单元的预览被三维过滤器隐藏。
- **关联逻辑/路径**：`src/MainWindow.cpp::build_assembly_model()`、`src/VtkViewer.cpp::set_mesh_file_impl()`、`src/VtkViewer.cpp::update_mesh_controls()`、巡览 `assembly_instance_contract`、`manual/test05.md` §3.2。
- **修复方案**：构建装配后在项目工作目录生成轻量 `assembly_preview.msh` 表面网格，显式加载到中央舞台并应用轴测视图；加载新网格时清除上一模型遗留的实体/单元类型过滤，默认维度改为从实际网格单元而非 OCC 实体列表推导；预览失败时清空旧舞台并明确提示，避免用单 Part 画面伪装装配成功。
- **验证结果**：构建通过；定向真实点击巡览 `assembly_instance_contract` 通过，自动断言预览文件已替换旧 Part、显示维度为 2，且装配表面实体数量符合两实例模型。
- **状态**：已修复；2026-09-15 用户随 `TEST-P5-G1-02` 人工验收确认通过

## 2026-09-15-002 钢板 Part 与装配实例沿用首个草图和 Part

- **影响接口/模块**：Phase 5 / G1 / `TASK-P5-01`；Part Feature 草图选择、Assembly 实例默认引用、装配几何尺寸。
- **现象**：混凝土和钢板分别按不同长宽及 20/5 的 Z 拉伸创建，但构建装配后两个实体的长、宽、高完全相同。
- **复现步骤**：依次从两个草图创建两个 Part；完成第一个 Part 拉伸后切换到第二个 Part，在未再次核对特征草图的情况下拉伸；连续创建两个 Assembly 实例并保留默认 Part；构建装配。
- **根因**：Part Feature 面板在切换 Part 时保留上一 Part 的草图下拉值；Assembly 新实例的默认 `part` 固定取 Parts 根下第一个节点。现场项目因此把 `part_plate/feature_2.sketch` 保存为 `sketch_concrete`，又把 `instance_plate.part` 保存为 `part_concrete`，最终导入了两次相同的混凝土 BREP。
- **关联逻辑/路径**：`src/PartFeaturePanel.cpp`、`src/MainWindow.cpp::default_params_for_kind()`、`src/MainWindow.cpp::assembly_instance_specs()`、巡览 `assembly_instance_contract`、`manual/test05.md` §3.1～3.2。
- **修复方案**：切换 Part 时让特征面板自动选择该 Part 保存的来源草图；创建 Assembly 实例时优先选择尚未被现有实例引用的 Part，同时保留主动复用同一 Part 的能力；人工说明增加构建前引用复核。
- **验证结果**：构建通过；定向真实点击巡览 `assembly_instance_contract` 通过。回归使用长宽和厚度均不同的两个 BREP，断言 Part 切换后草图选择正确，并直接校验装配中两个实例的三维包围盒尺寸不同。
- **状态**：已修复；2026-09-15 用户修正现有项目引用并随 `TEST-P5-G1-02` 人工验收确认通过

## 2026-09-15-003 Mesh 属性常规页摘要为空

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-05`；模型树 Mesh 节点、属性工作区“常规”页。
- **现象**：Mesh 节点已经生成且项目 YAML 中存在 `summary`、`physical_group_names` 和
  `sha256`，但切换到“属性 → 常规”后“摘要”仍固定显示 `-`。
- **根因**：`PropertyEditor::build_node_summary()` 仅实现 Features、Parts、Assembly 和
  Sketches 分支，没有消费 Mesh 节点已经持久化的摘要字段。
- **关联逻辑/路径**：`src/PropertyEditor.cpp::build_node_summary()`、
  `src/MainWindow.cpp` 巡览 `mesh_property_summary_contract`、`manual/test05.md` §4.1。
- **修复方案**：为 Mesh 增加常规摘要渲染，分行显示网格统计、Physical Group 名称和完整
  SHA-256；为摘要标签增加稳定对象名，并增加独立可定向运行的 UI 合同及既有清单合同断言。
- **验证结果**：构建通过；新增定向真实点击巡览 `mesh_property_summary_contract` 通过，
  自动断言常规页包含统计摘要、四个 Physical Group 名称和完整 SHA-256。
- **状态**：已修复；待用户复验 `TEST-P5-G1-05`

## 2026-09-15-004 保存成功反馈不明显且保存/另存为图标相同

- **影响接口/模块**：全局项目工具组；保存、项目另存为；Phase 5 人工验收操作。
- **现象**：点击保存后仅操作日志和底部状态栏出现短暂信息，用户难以确认是否成功；保存与
  项目另存为又使用完全相同的磁盘图标，工具栏中无法快速区分。
- **复现步骤**：打开已有项目，分别观察顶部保存/另存为图标并点击保存；主工作区没有明显
  成功反馈。
- **根因**：用户保存成功路径只调用 `statusBar()->showMessage()`，且两个 QAction 均绑定
  `IconGlyph::SaveDisk`。
- **关联逻辑/路径**：`src/MainWindow.cpp::show_project_saved_feedback()`、
  `src/MainWindow.cpp::MakeIcon()`、巡览 `project_save_feedback_contract`、`manual/test05.md`。
- **修复方案**：保存和另存为成功后在主工作区右上角显示约 3.5 秒的非模态确认浮层，包含
  对应动作与项目文件名；另存为改用磁盘加蓝色加号徽标，保留普通保存的原磁盘图标。
- **验证结果**：构建通过；定向真实点击巡览 `project_save_feedback_contract` 通过，自动断言
  保存与另存为图标像素不同、两类成功浮层均可见且包含对应动作和项目文件名；既有顶部
  工具栏合同 `l04_top_context_1280` 回归通过，并人工检查巡览截图确认浮层位置与可读性。
- **状态**：已修复；待用户复验

## 2026-09-15-005 Physical Group 面选择器无法辨认所属实例和作用面

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-05` / `manual/test05.md` §4.2；Mesh Workspace
  的 Physical Group 实体拾取、Assembly 装配舞台。
- **现象**：实体选择器只按 Gmsh tag 和 XYZ 包围盒平铺所有面。接触位置的混凝土顶面与
  钢板底面具有相同 Z 坐标，用户无法直接判断各面属于哪个 Assembly 实例；初版高亮又以
  “包围盒最薄轴”近似面法向，旋转实例的斜面和侧面会转到错误视角，并且模态选择框打开
  期间禁止操作主舞台，用户无法手工校正观察方向。
- **复现步骤**：构建包含 `instance_concrete` 与 `instance_plate` 的装配；进入“分组与网格场
  → 物理组”，维度选择 2 并点击“拾取”；观察两个接触面均显示相同接触高度，但列表没有
  实例归属，选择行也不改变舞台显示。
- **根因**：`GmshPanel::pick_entities_dialog()` 使用单列 `QListWidget`，没有利用 Assembly
  已生成的实例体组反查边界面的 owner；实体对话框与 `VtkViewer` 之间也没有临时预览通道。
  后续第一版自动视角仅比较 XYZ 包围盒跨度，不能表示非轴对齐曲面的真实法向；同时使用
  `QDialog::exec()`，Qt 会禁用父窗口及其中的 VTK 交互区。
- **关联逻辑/路径**：`src/GmshPanel.cpp::pick_entities_dialog()`、
  `src/GmshPanel.cpp::assembly_owner_for_entity()`、
  `src/VtkViewer.cpp::preview_mesh_entity()`、巡览 `entity_picker_guidance_contract` 与
  `assembly_instance_contract`、`manual/test05.md` §4.2。
- **修复方案**：选择器改为“实体 / 所属 Assembly 实例 / 几何范围”三列表格，按实例顶维
  Physical Group 反查每个面的 owner；新建组时清除列表刷新遗留的整实例实体文本，使候选
  默认均未勾选，编辑已有组时仍回显原实体。切换当前行时将完整装配降为灰色线框、关闭
  scalar 着色并以不透明亮黄色叠加候选面，同时根据候选面的所属实例、面中心和法向轴自动
  转到从面外侧约 45° 观察的视角；视角方向改为读取 Gmsh 曲面参数域中心的真实法向，并以
  所属实例中心校正外侧方向，参数化不可用时才回退到包围盒。选择器改为非模态工具窗并以
  局部事件循环等待结果，窗口打开期间主舞台仍可旋转、平移和缩放；接受、取消或关闭后清除
  预览并恢复原显示模式。移除遮挡舞台的大块行 tooltip，所有必要信息直接保留在三列中。
- **验证结果**：构建通过；`entity_picker_guidance_contract` 定向真实点击巡览通过，验证
  三列表头、范围、新建组默认无勾选、非模态状态、舞台交互说明与选择回写；
  `assembly_instance_contract` 通过，验证两个实例名均出现在所属列、旋转实例侧面的预览方向
  来自真实曲面法向、候选面触发黄色舞台预览、预览截图可生成，且对话框关闭后预览清除。
- **状态**：已修复；§4.2 人工验证通过（2026-09-16）

## 2026-09-15-006 Physical Group 统计表选中后无法更新或删除

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-05` / `manual/test05.md` §4.2；Mesh Workspace
  的 Physical Group 统计表、“更新所选”和“删除所选”。
- **现象**：统计表行已经呈蓝色/灰色选中，但顶部“分组”仍显示 `New`；修改实体后点击
  “更新所选”记录 `Update: select a physical group.`，点击“删除所选”记录
  `Delete: select a physical group.`，均不生效。
- **复现步骤**：在统计表直接单击 `contact_concrete` 行，不操作顶部下拉；重新拾取实体后
  分别点击“更新所选”和“删除所选”。
- **根因**：统计表 `itemSelectionChanged` 只发送 `physical_group_selected(dim, tag)` 给舞台
  高亮，没有同步 `phys_group_list_`；更新和删除处理函数却只从该下拉读取当前组，因此始终
  得到空 key（`New`）。
- **关联逻辑/路径**：`src/GmshPanel.cpp` 的 Physical Group 表格选择连接、
  `selected_physical_group_key()`、`on_physical_group_update()`、
  `on_physical_group_delete()`；巡览 `assembly_instance_contract`；`manual/test05.md` §4.2。
- **修复方案**：统计表改为单行选择，并统一通过 `select_physical_group()` 同步顶部下拉、
  名称、实体字段与舞台；更新/删除在下拉为空时额外从确实处于选中状态的统计表当前行读取
  `dim:tag`，避免平台事件时序造成失效；清空表格选择时同步回 `New`。
- **验证结果**：构建通过；`assembly_instance_contract` 定向真实点击巡览通过，自动执行
  “统计表选中 → 下拉/字段同步 → 改实体并更新 → 恢复实体 → 删除”，确认 Gmsh Physical
  Group 与项目持久化 JSON 同步删除，再重建该组并完成原有保存重开回归。
- **状态**：已修复；§4.2 人工验证通过（2026-09-16）

## 2026-09-15-007 Physical Group 拾取只显示黄色候选面而缺少整体位置

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-05` / `manual/test05.md` §4.2；Physical Group
  舞台过滤、实体拾取预览。
- **现象**：在统计表选择 `fixed_bottom` 等组后，舞台只显示该组；随后点击“拾取”并选择
  候选面，舞台只剩黄色候选面，看不到完整装配的灰色线框背景，无法判断整体位置；界面也
  没有单独取消舞台过滤的入口。
- **复现步骤**：在 Physical Group 统计表选择任一单面组，使舞台过滤到该组；保持该组为
  编辑目标并点击“拾取”，再单击任一候选面。
- **根因**：Physical Group 当前组同时承担“编辑目标”和“VTK 舞台过滤”两种状态；打开
  拾取器前没有清除既有 `set_mesh_group_filter()`，临时黄色预览只能叠加到已过滤的单面上。
- **关联逻辑/路径**：`src/GmshPanel.cpp` 的 `physicalGroupEntityPickButton`、
  `physicalGroupClearStageFilterButton`，`src/VtkViewer.cpp::set_mesh_group_filter()`、
  `preview_mesh_entity()`；巡览 `assembly_instance_contract`；`manual/test05.md` §4.2。
- **修复方案**：点击“拾取”时自动发送舞台过滤清除信号，但不修改顶部组下拉、名称或实体
  字段，使当前组仍可执行“更新所选”；新增“取消舞台筛选”按钮供用户随时恢复完整模型，
  同样不取消当前编辑目标。随后黄色候选面叠加到完整装配灰色线框上。
- **验证结果**：构建通过；`assembly_instance_contract` 定向真实点击巡览通过。回归先人为
  启用 Assembly 表面组过滤，再分别验证按钮清除和点击拾取自动清除，并断言编辑组不变、
  拾取窗口中舞台处于未过滤状态，既有黄色预览、真实法向和保存重开合同继续通过。
- **状态**：已修复；§4.2 人工验证通过（2026-09-16）

## 2026-09-15-008 取消实体拾取或舞台筛选后模型变为红绿拼色

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-05` / `manual/test05.md` §4.2；Physical Group
  舞台过滤清理、实体拾取预览退出。
- **现象**：实体拾取过程中完整装配为灰色线框且候选面为黄色；点击“取消”返回，或直接点击
  “取消舞台筛选”后，舞台虽然恢复实体显示，却变成红色主体与绿色边界面混合的颜色。
- **复现步骤**：在统计表选中任一二维 Physical Group（例如 `fixed_bottom`），随后点击
  “取消舞台筛选”；或进入实体“拾取”、选择候选面后点击“取消”。
- **根因**：`set_mesh_group_filter()` 选择二维组时会把网格维度同步为 2；清除组过滤只把
  Group 下拉恢复为 `All`，没有把维度恢复为模型最高维。于是新增的 `contact/fixed/load`
  二维物理组继续按 `phys_id` 覆盖显示，形成红绿拼色。同时原清理动作没有完整重置实体与
  单元选择状态，可能继续保留独立选择层。
- **关联逻辑/路径**：`src/VtkViewer.cpp::set_mesh_group_filter()`、网格拾取 Clear 动作、
  `src/GmshPanel.cpp` 的 `physicalGroupClearStageFilterButton` 与实体拾取入口；巡览
  `assembly_instance_contract`；`manual/test05.md` §4.2。
- **修复方案**：负值组筛选统一解释为“恢复完整实体舞台”：Group 与 Entity 均切回 All，
  清空 group/entity/cell 选择，并把维度恢复为当前网格实际包含单元的最高维（轻量表面预览
  为 2D，正式体网格为 3D）；实体拾取前、拾取退出以及
  “取消舞台筛选”复用该清理路径。网格拾取 Clear 同步清除实体选择，避免选择 actor 残留。
- **验证结果**：构建通过；`assembly_instance_contract` 定向真实点击巡览通过，自动断言
  “取消舞台筛选”后和拾取取消后均恢复进入组筛选前的最高单元维度，编辑器当前 Physical Group 保持不变，
  黄色临时预览已清除。
- **状态**：已修复；§4.2 人工验证通过（2026-09-16）

## 2026-09-16-009 Physical Group 更新/重建结果不可辨认且操作后舞台被单面过滤

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-05` / `manual/test05.md` §4.2；Physical Group
  新增、更新、删除、统计表与舞台过滤。
- **现象**：把 `contact_concrete` 的成员从原面改为 `2:12` 后，统计表仍显示 `2:5` 且单元数
  为 0，用户判断更新失败；删除 `contact_plate` 后重新选择面 `2:5` 并添加，新组显示为
  `2:9 contact_plate`，同时舞台又切换到单面/边界着色状态。
- **复现步骤**：选中组 `2:5 contact_concrete`，拾取面 `2:12` 并点击“更新所选”；再删除
  `contact_plate`，以面 `2:5` 同名重建并观察顶部组下拉、统计表和舞台。
- **根因**：UI 把 Physical Group tag 与成员 entity tag 都显示为 `维度:编号`，统计表却只
  展示组 tag，没有展示成员实体；用户因此把保持不变的组标识误认为未更新。删除后 Gmsh
  按其 tag 分配规则为新组生成 `2:9`，这与成员面 `2:5` 属于不同命名空间，本身不是数据
  错误。另一方面，新增/更新后刷新当前组会再次触发舞台组过滤，导致完整模型被单面替换。
  已生成网格的单元数也不会因几何组编辑自动重算，且项目文件要到用户保存时才写盘。
- **关联逻辑/路径**：`src/GmshPanel.cpp::on_physical_group_add()`、
  `on_physical_group_update()`、`on_physical_group_delete()`、
  `update_physical_group_list()`、`update_physical_group_table()`；巡览
  `assembly_instance_contract`；`manual/test05.md` §4.2。
- **修复方案**：统计表增加“成员实体”列并将原“标识”明确命名为“组标识”；顶部下拉改为
  `组 2:9 · contact_plate` 格式。新增和更新成功后显示组标识、成员实体、重新网格化及保存
  提示；新增/更新/删除完成后统一清除舞台组过滤，同时保留新增或更新的组为编辑目标。
- **验证结果**：构建通过；`assembly_instance_contract` 定向真实点击巡览通过。回归自动执行
  “成员更新 → 内存持久化 JSON 校验 → 删除 → 同名重建”，断言成员列显示新实体、JSON
  保存新几何签名、成功提示包含成员编号，并确认更新和重建后舞台均未残留组过滤。
- **状态**：已修复；§4.2 人工验证通过（2026-09-16）

## 2026-09-16-010 Physical Group 刷新后完整装配退化为单色

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-05` / `manual/test05.md` §4.2；Physical Group
  清单刷新与主舞台实例着色。
- **现象**：生成 3D 网格后，主舞台能以不同颜色区分两个 Assembly 实例；在物理组页点击
  “刷新”后，舞台退化为单色，用户无法继续辨认两个实例。
- **复现步骤**：生成包含两个体实例和二维作用面组的 3D 网格；在顶部“分组”保留任一二维
  组作为编辑目标，点击“刷新”，观察主舞台。
- **根因**：`update_physical_group_list()` 为同步当前编辑字段会重新调用
  `on_physical_group_selected()`；刷新入口没有区分“编辑目标”和“舞台筛选”，因此把保留的
  二维组再次发送给 `VtkViewer::set_mesh_group_filter()`。舞台离开完整三维实例视图后，原有
  的实例 `phys_id` 颜色区分也随之丢失。这不是预期设计。
- **关联逻辑/路径**：`src/GmshPanel.cpp::on_physical_group_refresh()`、
  `update_physical_group_list()`、`src/VtkViewer.cpp::set_mesh_group_filter()`；巡览
  `assembly_instance_contract`；`manual/test05.md` §4.2。
- **修复方案**：刷新仍保留当前物理组作为编辑目标并同步名称、成员字段，但刷新完成后统一
  清除临时实体预览和舞台组筛选，恢复最高维完整模型；颜色数组自动范围的两个端点改为原子
  更新，避免刷新过程中把半更新的反向区间提交给 VTK；增加稳定对象名供巡览直接点击。
- **验证结果**：构建通过；`assembly_instance_contract` 定向真实点击巡览通过，自动断言
  “刷新前后编辑组不变、舞台无组筛选、网格维度不变”。
- **状态**：已修复

## 2026-09-16-011 Physical Group 新建态与单元数在刷新/重开后丢失

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-05` / `manual/test05.md` §4.2；Physical Group
  下拉、统计表选择、网格清单持久化与项目重开。
- **现象**：中文界面切换到“新建”后点击刷新，下拉文字变为 `New`；编辑字段虽然已清空，
  统计表仍以灰色保留原行；生成网格后统计表单元数正确，但刷新或保存重开后全部变为 0。
- **复现步骤**：选中任一物理组行后把顶部分组切到“新建”，再点击刷新；随后生成网格并
  记录各组单元数，分别执行物理组刷新以及保存、重开项目。
- **根因**：动态重建下拉时写死英文 `New`，没有经过本地化；空 key 分支只清空表单字段，
  没有清理 `QTableWidget` 的 selection/current index，灰色只是 macOS 对失焦选择的默认呈现。
  单元数则每次都从当前 Gmsh 模型查询，而生成网格运行于独立模型/子进程，完成后会恢复仅
  含 OCC 几何的模型，其离散单元数必然为 0；项目中已持久化的 `mesh_snapshot` 未回注表格。
- **关联逻辑/路径**：`src/GmshPanel.cpp::on_physical_group_selected()`、
  `update_physical_group_list()`、`update_physical_group_table()`、
  `set_physical_group_manifest()`，`src/MainWindow.cpp` 的 `mesh_manifest` 消费与
  `load_project()`；巡览 `assembly_instance_contract`；`manual/test05.md` §4.2。
- **修复方案**：动态“新建”项统一走 `l10n::tr()`；进入新建态同时清除表格选择和当前索引，
  并统一表格 active/inactive 选择强调色。GmshPanel 缓存最近生成或项目加载的物理组单元数，
  表格以按稳定名称匹配的正式网格清单计数优先、当前模型即时值兜底；新增、更新、删除组或重建 Assembly
  时清空缓存，避免展示过期网格计数。生成网格和项目加载均重新注入持久化清单。
- **验证结果**：构建通过；`assembly_instance_contract` 定向真实点击巡览通过。回归自动验证
  中文“新建”在刷新后不回退英文、进入新建态后 selection/current index 均为空、失焦选择仍
  使用统一强调色，以及正式网格清单计数在刷新和项目保存重开后保持非零。
- **状态**：已修复

## 2026-09-16-012 实体拾取的点和边缺少可辨认舞台高亮

- **影响接口/模块**：Mesh Workspace 的通用实体拾取器；Physical Group、网格场与几何操作
  共用的点/边/面/体选择预览。
- **现象**：面和体候选能在灰色装配背景上显示为黄色，但选择点或边时舞台没有可辨认的
  黄色提示，用户只能根据列表坐标猜测目标。
- **根因**：四个拓扑维度共用同一个 VTK 选择 Actor，属性固定为面用的 Surface 表示；第一版
  仅增大 OpenGL 点尺寸和线宽，点和边仍与灰色线框完全共面，macOS 深度缓冲会继续遮住它们。
  第二版虽然生成了球体/管体，但仍以 VTK 网格的 `entity_dim/entity_tag` 阈值作为输入；项目含
  Physical Groups 时，`.msh` 可只保留被物理组覆盖的面/体单元，OCC 点和边没有低维网格单元，
  因而实际项目中的选中输入为空，而包含全维单元的自动化夹具产生了假通过。
- **修复方案**：点预览生成半径随模型尺度变化的真实黄色球体，边预览通过 TubeFilter 生成
  有半径的真实黄色管体，并直接从当前 Gmsh 几何读取点坐标、按曲线参数域离散边，不再要求
  舞台网格包含 0D/1D 单元；点按实例中心外向观察，边按参数域中点切线的法平面计算外侧视角。
  面和体继续使用不透明黄色表面，帮助文字同步说明四种视觉语义。
- **验证结果**：`assembly_instance_contract` 对本次新增的 0D/1D 预览逐一断言筛选输出非空、
  Actor 可见且使用对应表示和最小可读尺寸；既有选择器合同继续覆盖面/体行为。
- **状态**：已修复

## 2026-09-16-013 Contact 高级参数可绕过二维主从面校验

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-06` / `manual/test05.md` §5.2；Contact
  属性弹窗的参数页、校验页和“确定”提交路径。
- **现象**：主/从面快捷下拉只显示二维 Physical Group，且主从面相同时能明确报错；但在
  “高级参数”中把 `primary` 手工改为三维体组 `instance_plate` 后，参数页没有错误提示，
  校验页显示“无校验问题”，“确定”也会接受并持久化该错误引用。
- **复现步骤**：打开 `contact_plate_concrete`；勾选“高级参数”；把 `primary` 从
  `contact_plate` 改为 `instance_plate`；依次观察参数页、校验页并点击“确定”；重开对象。
- **根因**：快捷表单通过只读下拉约束候选，但高级参数表允许直接写任意字符串；三条弹窗
  路径共用的 `PropertyEditor::validate_params()` 只检查主从面非空且不同，没有再次核对
  `primary/secondary` 是否属于当前二维边界组清单。工作流级预检虽另有网格维度检查，却不
  参与属性弹窗提交，因此无法阻止错误值写入模型树。
- **关联逻辑/路径**：`src/PropertyEditor.cpp::validate_params()`、
  `FloatingPropertyForm::commit_if_valid()`、巡览 `contact_load_mapping_contract`、
  `manual/test05.md` §5.2。
- **修复方案**：在 Contact 公共参数校验入口对 `primary`、`secondary` 执行二维 Physical
  Group 成员校验，输出可定位到具体字段的错误；参数页即时提示、校验页汇总和“确定”阻断
  继续复用同一结果。定向合同通过真实浮动属性弹窗从高级参数表写入 `instance_plate`，断言
  两处提示均出现、点击“确定”后弹窗仍保留且原模型节点不被污染。
- **验证结果**：构建通过；`contact_load_mapping_contract` 定向真实点击巡览通过。合同实际
  打开 Contact 浮动属性表单，经高级参数表写入 `primary=instance_plate`，确认参数页与校验页
  均报告二维 Physical Group 错误；点击“确定”后弹窗保持打开，原模型树节点仍为
  `primary=contact_plate`。
- **人工回归（2026-09-17）**：用户在 `phase5-g1-assembly-contact.gmp.yaml` 中复验三条
  路径全部通过：高级参数改 `primary=instance_plate` 后参数页即时报错；点击“确定”弹窗
  保持打开且未写回；“取消”后重开主面仍为 `contact_plate`；校验页汇总列出同一错误。
- **状态**：已修复，人工回归通过

## 2026-09-16-014 浮动 Loads 属性窗缺失活动档案的 Pressure 类型与模板

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-07` / `manual/test05.md` §5.3 第 5 步；
  `Loads` 浮动属性窗与活动应用档案的类型候选同步。
- **现象**：活动应用为 `DamSafetyApp-opt`、mapping 为 `1.0.0`，但双击
  `pressure_mapping_check` 后，“模板”只有 Body Force 等通用项，没有“面压力”；类型也停留
  在 `BodyForce`，保存副本后生成报告将该对象列为 `[Kernels]`。
- **复现步骤**：在 M-01 项目另存的 Pressure 测试副本中删除 `load_disp_z`，添加
  `Loads/pressure_mapping_check` 并双击打开浮动属性窗，检查模板和类型候选。
- **根因**：主界面 `PropertyEditor` 已由 `push_context_to_moose_panel()` 接收档案/mapping
  驱动的 `Pressure` 候选；`FloatingPropertyForm` 则新建另一份 `PropertyEditor` 操作克隆树，
  只注入物理组和单位，未注入 Physics、Loads、Interactions 的档案候选。因此之前的自动合同
  检查了主编辑器，却漏掉了用户实际使用的浮动窗。
- **关联逻辑/路径**：`src/MainWindow.cpp::open_property_form()`、
  `src/FloatingPropertyForm.cpp`、`src/PropertyEditor.cpp::build_form_for_kind()`、
  巡览 `contact_load_mapping_contract`、`manual/test05.md` §5.3。
- **修复方案**：创建浮动窗时把当前活动档案的 Physics、Loads、Interactions 候选传给克隆
  编辑器，并在构建对象表单之前注入；扩展定向巡览以实际打开 Pressure 浮动窗，检查稳定模板
  标识 `Surface Pressure` 和类型候选，再点击模板确认类型变为 `Pressure`。中文界面模板文字为
  “面压力”，人工文档同时标注中英文名称及已有 `BodyForce` 对象的续做方式。
- **验证结果**：构建通过；`contact_load_mapping_contract` 定向真实点击巡览通过。此前已保存的
  `pressure_mapping_check` 仍为 `BodyForce`，需用修复后的程序重开并手动应用“面压力”模板；
  不自动改写用户项目。
- **人工回归（2026-09-17）**：TEST-P5-G1-07 C3 中，用户在 Pressure 测试副本的浮动
  Loads 属性窗成功应用“面压力”模板，类型变为 `Pressure`，变量候选含并选中 `disp_z`，
  生成 `.i` 的 `[BCs/pressure_mapping_check]` 与生成报告来源条目均正确。
- **状态**：已修复，人工回归通过

## 2026-09-16-015 Pressure 变量候选缺少 disp_z，模板误设为 u

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-07` / `manual/test05.md` §5.3 第 5 步；
  `Loads/Pressure` 表单、模板及参数校验。
- **现象**：在已恢复 `fixed_bottom`、`load_top` 物理组且已有三个位移 BC 的项目中，
  `Pressure` 的变量下拉只显示 `u`；同步生成的 `.i` 因而可能写入 `variable = u`。
- **根因**：Loads 表单的变量候选只读取 `Variables` 树节点，而当前项目该节点为空；
  Pressure 类型默认值和“面压力”命名模板也复用通用 Loads 的 `u` 回退值。
  BC 表单已有 `disp_x/disp_y/disp_z` 候选，两处规则不一致。
- **修复方案**：Pressure 候选包含显式 Variables 和标准位移变量
  `disp_x/disp_y/disp_z`；Pressure 类型默认值及“面压力”模板默认 `disp_z`；
  从 `BodyForce` 直接切换类型为 Pressure 时，也将旧变量改为 `disp_z`；
  未在候选中声明的变量（包括本项目的 `u`）在表单参数校验时明确报错。
  定向巡览复现旧 `variable=u` 后分别应用模板和直接切换类型，断言浮动窗显示并选中 `disp_z`。
  人工文档明确 `Variables` 根节点可暂空，且 `u` 不可作为本压力用例的变量。
- **验证结果**：构建通过；`contact_load_mapping_contract` 定向真实点击巡览通过。
  巡览断言 Pressure 候选包含 `disp_z`，模拟旧 `variable=u` 后在浮动窗应用
  “面压力”模板、或直接切换 Pressure 类型，均会选中 `disp_z`；未声明变量触发表单参数校验。
- **人工回归（2026-09-17）**：TEST-P5-G1-07 C3 中，Pressure 变量下拉列出并选中
  `disp_z`（非 `u`），模板字段（factor/component/use_displaced_mesh）正确应用。
- **状态**：已修复，人工回归通过

## 2026-09-16-016 Pressure 高级参数可绕过二维作用面校验

- **影响接口/模块**：Phase 5 / `TEST-P5-G1-07` / `manual/test05.md` §5.3 第 8 步；
  `Loads/Pressure` 浮动属性窗、参数校验与模型同步预检。
- **现象**：在高级参数表把 `boundary` 从二维组 `load_top` 改为三维体组
  `instance_plate` 后，属性窗校验页显示“无问题”，点击“确定”可写回非法值。
- **根因**：工作流级校验已有按网格维度检查 Physical Group 的逻辑，但
  `PropertyEditor::validate_params("Loads", …)` 对 Pressure 仅检查 `boundary`
  非空；高级参数表可绕过快捷表单的二维候选限制。浮动窗提交与模型同步入口均复用
  这条不完整的参数校验。
- **关联逻辑/路径**：`src/PropertyEditor.cpp::validate_params()`、
  `FloatingPropertyForm::commit_if_valid()`、`MainWindow::sync_model_to_input()`、
  巡览 `contact_load_mapping_contract`。
- **修复方案**：Pressure 的每个 `boundary` 标识都须存在于当前二维边界组候选中；
  错误显示在参数页和校验页，阻止浮动窗确认。同步入口复用同一规则，阻止旧项目中
  已保存的非法 Pressure 写入 `.i`。人工文档区分弹窗内阻断与旧项目持久非法状态的
  全局预检。
- **验证结果**：构建通过；`contact_load_mapping_contract` 定向真实点击巡览通过。
  巡览从浮动窗高级参数表写入 `instance_plate`，断言两处校验提示、确认不写回；
  再将非法值直接注入模型树，断言同步被阻断且现有输入不变。
- **人工回归（2026-09-17）**：TEST-P5-G1-07 C3 负向验证中，高级参数把 `boundary`
  改为 `instance_plate` 后参数页即时报错、校验页列出同一错误、点击“确定”弹窗保持打开
  且未写回、“取消”后恢复 `load_top`。
- **状态**：已修复，人工回归通过

## 2026-09-16-017 M-01 各向同性线弹性材料类型缺失于下拉

- **影响接口/模块**：Phase 5 / M-01 / `manual/test05.md` §6.1；Material 浮动属性窗与
  普通线弹性材料参数。
- **现象**：活动档案的 mapping 支持 `ComputeIsotropicElasticityTensor`，但材料“类型”
  下拉没有此项；原操作说明要求在高级参数表手改类型和逐行添加 E/ν，难以按正常表单执行。
- **根因**：`PropertyEditor::build_form_for_kind("Materials")` 仍使用早期通用材料类型清单，
  只给 `AbaqusCDP` 展示 Young's Modulus/Poisson's Ratio 快捷字段；未把 mapping 已支持的
  普通各向同性弹性类型纳入候选和表单显示规则。
- **关联逻辑/路径**：`src/PropertyEditor.cpp`、`src/MainWindow.cpp` 的
  `g1_isotropic_material_form_contract`、`manual/test05.md` §6.1。
- **修复方案**：加入类型候选；普通线弹性展示 E（MPa 输入、Pa 存储）、ν 和 Block，
  类型切换时清除默认 `prop_names/prop_values`；校验 E/ν 必填及数值范围。6.1 文档逐节点
  列明混凝土和钢板的快捷表单值、存储值以及两个应力计算节点的 Block。
- **验证结果**：构建通过；定向真实点击巡览验证主编辑器和浮动属性窗的候选、字段、
  单位换算、重开回显及生成块；既有 `cdp_material_form_contract` 定向巡览通过。
- **状态**：已修复，待人工回归

## 2026-09-16-018 材料 Block 作用区域自由输入易产生无效体组

- **影响接口/模块**：Phase 5 / M-01 / `manual/test05.md` §6.1；材料属性窗、材料参数校验与模型同步。
- **现象**：普通线弹性材料的 `Block/作用区域` 是自由输入框，无法从当前三维物理体组中选择；拼错名称或高级参数写入不存在的体组时仍可能保存。
- **根因**：已有的体组多选控件仅对 Section/Physics 等节点启用，Materials 未接入；材料参数校验也未核对 `block` 是否存在于当前体组清单。
- **关联逻辑/路径**：`src/PropertyEditor.cpp`、`src/MainWindow.cpp::sync_model_to_input()`、巡览 `g1_isotropic_material_form_contract`、`manual/test05.md` §6.1。
- **修复方案**：Materials 复用可选物理体多选与应用按钮，`block` 快捷字段只读回显；非空 `block` 的每个名称必须属于当前物理体组，覆盖高级参数与旧项目；非法体组阻断确认及模型同步。
- **验证结果**：构建通过；`g1_isotropic_material_form_contract` 定向真实点击巡览通过，覆盖多体组选择、只读回显、高级参数非法值阻断浮动窗确认、旧项目式非法值阻断模型同步；既有 `cdp_material_form_contract` 定向巡览通过。待人工回归。
- **状态**：已修复，待人工回归

## 2026-09-17-019 项目重开后校验物理组清单未恢复导致 Materials/Contact 误报

- **影响接口/模块**：Phase 5 / TEST-P5-G1-04 / `manual/test04.md` §4.2、`manual/test05.md` §5.x；项目重开后的工作流校验（状态区 Workflow 汇总与 Job > Validate Workflow）与属性弹窗校验。
- **现象**：含持久化 `mesh_snapshot` 的装配项目（如 phase5-g1-assembly-contact）纯重开后，用户未进入 Mesh 工作窗、未点“构建装配”，状态区即显示 `Workflow blocked`：Materials `block must reference an existing volume Physical Group`、Interactions `primary/secondary must reference an existing 2D Physical Group`，并伴有 Sections `material reference`、Assembly `part reference` 误报；手动点“构建装配”再“校验工作流”后全部消失。
- **复现步骤**：1) 直接重开含 8 个物理组快照的项目；2) 不操作任何界面，观察状态区工作流校验；3) 打开 Job > Validate Workflow 可见上述误报。
- **根因**：`PropertyEditor::validate_params()` 依赖两类瞬态 UI 状态而非项目持久化数据：a) 体/面组缓存 `volume_groups_/boundary_groups_` 由 GmshPanel 发射与加载尾部按 `mesh_snapshot_` 喂食共同维护，重开过程中 `restore_external_models()` 在空模型/单 Part 模型时刻（`src/GmshPanel.cpp:1444-1449` 空模型分支、`activate_model()` 导入单 Part brep 后）会发射空/不完整清单，最后一次校验若落在喂食之前即误报；b) `collect_model_names()` 仅从 `current_item_->treeWidget()` 取模型树，项目重开后未选中任何节点时返回空，Sections.material / Assembly.part 引用被误判悬空（实测纯重开后稳定复现 4 条此类误报）。
- **关联逻辑/路径**：`src/PropertyEditor.cpp::validate_params/collect_model_names`、`src/MainWindow.cpp::collect_workflow_issues/load_project`、`src/GmshPanel.cpp::update_physical_group_list/restore_external_models/build_assembly`。
- **修复方案**：a) 新增 `MainWindow::sync_property_editor_groups_from_snapshot()`，`collect_workflow_issues()` 开头把 PropertyEditor 组缓存对齐到持久化 `mesh_snapshot_`（仅在有清单且不一致时写入；无清单时保留 GmshPanel 实时发射的候选，网格生成后的正常发射不变）；b) `PropertyEditor` 新增 `set_model_tree()` 持有稳定模型树句柄（MainWindow 创建编辑器时注入），`collect_model_names()` 在 `current_item_` 为空或已脱离树时回退到该句柄。
- **验证结果**：构建通过；临时探针确认重开 phase5-g1-assembly-contact 后状态区由 `Workflow blocked: 4 error(s)` 变为 `Workflow ready` 且 `collect_workflow_issues()` 无任何误报（探针已移除）；新增定向巡览 `project_reopen_group_contract` 通过；既有 `assembly_instance_contract`、`contact_load_mapping_contract` 定向巡览通过；`ctest` 通过。
- **人工回归（2026-09-17）**：用户重启应用后直接打开 phase5-g1-assembly-contact，未点“构建装配”，直接执行“校验工作流”，报告显示 `Workflow ready: no blocking issue found.`，误报不再出现。
- **全量巡览跟进（2026-09-17）**：提交前全量真实点击巡览暴露两处巡览夹具隔离问题（非产品缺口；产品层新建项目已在 `create_fresh_project` 清空 `mesh_snapshot_`，临时探针确认全量下 sync 把注入候选覆盖为快照组）：`g1_isotropic_material_form_contract` 与 `contact_load_mapping_contract` 都是“注入合成组候选后再 `add_child_item`”的结构，树变更触发的工作流校验把编辑器组缓存对齐回前序网格步骤留下的快照组。按新建项目的清理逻辑在这两个步骤 setup 保存并复位 `mesh_snapshot_`、cleanup 恢复；`sync_property_editor_groups_from_snapshot()` 对真实路径的保护未削弱。修复后全量 113 步巡览 exit=0。
- **状态**：已修复，人工回归通过

## 2026-09-17-020 Feature 节点常规页摘要缺少 brep 路径回显

- **影响接口/模块**：Phase 5 / TEST-P5-G1-01；Feature（Part 特征）属性窗常规页摘要与参数页。
- **现象**：Feature 节点常规页摘要（`src/PropertyEditor.cpp:1680-1695` `build_node_summary()`）不显示 brep 路径，参数页也无 brep 快捷字段；人工验收 TEST-P5-G1-01 时需直接打开项目 YAML 才能核对 BREP 来源与归属。
- **根因**：`build_node_summary()` 的 Feature 分支只汇总来源草图与特征参数，未纳入 brep；Feature 表单快捷字段集合也未包含 brep 只读项。
- **关联逻辑/路径**：`src/PropertyEditor.cpp::build_node_summary/build_form_for_kind`、`manual/test04.md` TEST-P5-G1-01。
- **修复方案**（建议，未实施）：常规页摘要补充只读 brep 路径显示，并标注“当前生效/历史”状态（与 Part 现行 brep 是否一致）；参数页增加只读 brep 快捷字段，避免验收时翻阅 YAML。
- **验证结果**：未实施，无。
- **状态**：已登记，待排期

## 2026-09-17-021 项目打开后主窗口标题栏仍显示 Untitled

- **影响接口/模块**：通用 UI / 项目打开；主窗口标题栏。
- **现象**：启动后直接打开既有项目（如 phase5-g1-assembly-contact.gmp.yaml），状态栏项目路径正确，但主窗口标题栏仍显示 “GMP-ISE - Untitled”；“另存为”路径下标题可正常更新。
- **复现步骤**：启动应用 → 打开既有项目 → 观察主窗口标题栏。
- **根因**：窗口标题由 `MainWindow::update_window_title()`（`src/MainWindow.cpp:12319`）按 `project_path_` 刷新，但其常见触发点 `set_project_dirty()` 在脏标记未变化时提前返回；项目加载尾部只调用 `set_project_dirty(false)` 与 `update_project_status()`，启动后首次打开项目时脏标记本就为 false，标题刷新被跳过。
- **关联逻辑/路径**：`src/MainWindow.cpp::load_project/update_window_title/set_project_dirty`。
- **修复方案**：项目加载尾部在 `set_project_dirty(false)` 后显式调用 `update_window_title()`。
- **验证结果**：构建通过；`project_reopen_group_contract`、`project_save_feedback_contract` 定向真实点击巡览通过。待人工回归（打开项目后标题应显示项目文件名）。
- **人工回归（2026-09-17）**：用户重启应用后直接打开 `phase5-g1-assembly-contact.gmp.yaml`，标题栏显示项目文件名；随后“项目另存为”`phase5-g1-pressure-mapping-check.gmp.yaml`，标题栏同步切换为新文件名。
- **状态**：已修复，人工回归通过

## 2026-09-17-022 舞台变量面板选中 phys_id 时缺少 ID→组名图例

- **影响接口/模块**：通用 UI / 网格与结果查看；中央舞台左侧“变量”面板与色标。
- **现象**：网格视图按 `Cell: phys_id` 着色时，色标只显示数值范围（如 2～8），用户无法直接从舞台知道某个颜色/ID 对应哪个 Physical Group，需要手动对照网格清单或项目 YAML。
- **根因**：`phys_id`/`phys_dim` 单元数组在网格加载时由 Gmsh 物理组生成（`src/VtkViewer.cpp:601-681`），生成时刻的 ID→名称映射没有保留到查看层，变量面板也没有二级信息区。
- **关联逻辑/路径**：`src/VtkViewer.cpp`（变量列表、色标、`phys_id` 数组）、`mesh_snapshot_` 物理组清单（名称/维度/单元数真源）。
- **修复方案**（建议，未实施）：变量面板选中 `phys_id`（或 `phys_dim`）时，在列表下方显示二级信息区：ID → 组名 → 维度/单元数（数据取自项目 `mesh_snapshot_` 清单，不依赖 Gmsh 会话状态）；色标或图例同步标注组名。
- **验证结果**：未实施，无。
- **状态**：已登记，待排期

## 2026-09-17-023 舞台缺少“装配预览 / 体网格”视图来源切换

- **影响接口/模块**：通用 UI / 装配与网格查看；中央 VTK 舞台。
- **现象**：舞台始终显示“最后加载的网格”：特征操作后显示 Part 网格，“构建装配”后显示装配表面预览（面组 phys_id 2～8 着色），生成或点击 Mesh 节点后显示体网格（体组 phys_id 1～3 着色）。从体网格视图想回到装配预览视图时，没有直接的切换入口，只能再次点击“构建装配”重新生成预览。
- **根因**：舞台是单一 VTK 网格视图，预览与网格均由“最后写入者”加载（`src/MainWindow.cpp:1739、8488`），没有视图来源模型或切换命令。
- **关联逻辑/路径**：`src/MainWindow.cpp::build_assembly_model`（装配预览加载）、`src/VtkViewer.cpp::set_mesh_file_from_current_model`、Mesh 节点选择加载路径。
- **修复方案**（建议，未实施）：在舞台工具或装配/网格模块提供显式的舞台数据源切换（如“装配预览 / 活动网格”二选一按钮或下拉），切换时直接加载已存在的 `assembly_preview.msh` 或活动网格文件，不触发重新构建；预览文件缺失时禁用并提示。
- **验证结果**：未实施，无。
- **状态**：已登记，待排期

## 2026-09-17-024 另存为副本的网格路径仍指向原项目网格文件

- **影响接口/模块**：Phase 5 / `manual/test05.md` §5.3（Pressure 测试副本）；项目另存为、网格路径迁移与项目自包含性。
- **现象**：将 `phase5-g1-assembly-contact.gmp.yaml` 另存为 `phase5-g1-pressure-mapping-check.gmp.yaml`（保存在不同目录）后，副本的 Mesh 节点与生成 `.i` 的 `[Mesh/file]` 仍指向原项目的 `.work/case/phase5-g1-assembly-contact/mesh_assembly_g1.msh`，副本不自包含；若在副本中执行“生成网格”，输出会覆写原项目的网格文件。
- **复现步骤**：1) 打开含装配网格的项目并另存为新文件名/新目录；2) 在副本中同步 MOOSE 输入；3) 观察 `[Mesh/file]` 路径仍为原项目路径。
- **根因**：`MainWindow::migrate_project_mesh_paths()`（`src/MainWindow.cpp:11760`）只把 `is_legacy_default_mesh_path()` 命中的旧版共享 `out/` 默认路径迁移进项目自有 `.work/case/<项目名>/`；指向**其他项目**工作目录的网格路径被视为“用户明确选择的外部路径”原样保留，另存为路径（`src/MainWindow.cpp:12119`）同样只走这一套迁移逻辑。
- **关联逻辑/路径**：`src/MainWindow.cpp::migrate_project_mesh_paths/is_legacy_default_mesh_path/save_project(as)`、TASK-P4-CLOSE-02“Mesh/Job/输入/报告统一指向项目自有 `.msh`”。
- **修复方案**：新增文件级辅助 `enclosing_case_work_dir()`（`src/MainWindow.cpp:187`），解析路径所属的 `.work/case/<X>/` 目录；`migrate_project_mesh_paths()` 对每个 Mesh 节点路径判定：命中 legacy `out/` 或“位于某个 `.work/case/<X>/` 但 `X` 不是当前项目自己的工作目录”（与 `project_case_work_dir(project_path)` 比较，相同则快速短路不处理）时，按既有 legacy 同款逻辑迁移——复制文件到当前项目工作目录（目标已存在则不覆盖），重定向 Mesh 节点 `path`，并联动 `mesh_snapshot_.mesh_path`、gmsh 输出路径、`moose_panel_` 的 mesh_path 及 `input_text/structured_input/generation_report` 文本替换。源文件不存在时仍重定向到当前项目工作目录目标路径，并写操作日志说明源缺失未复制；`.work/case/` 之外的用户外部路径保持不动。
- **验证结果**：构建通过；新增定向巡览 `project_save_as_mesh_migration_contract` 通过（构造项目 A 含工作目录网格 → 另存为 B → 断言副本 Mesh 节点路径、mesh_snapshot_、gmsh 输出路径、moose mesh_path 与 `.i` 的 `[Mesh/file]` 均指向 B 的 `.work/case/saveas_b/`、网格文件已复制且内容一致、A 的网格文件内容不变）；既有 `project_context_isolation_contract`、`project_save_feedback_contract`、`project_reopen_group_contract` 定向巡览通过；`ctest` 通过。
- **人工回归（2026-09-17）**：用户重启应用后直接打开既有副本 `phase5-g1-pressure-mapping-check.gmp.yaml`，日志显示 `Mesh output migrated into project workspace: .../0917/.work/case/phase5-g1-pressure-mapping-check/mesh_assembly_g1.msh`；同步后 `.i` 的 `[Mesh/file]` 指向副本自有工作目录；主项目与副本网格文件 SHA-256 均为 `f87c1606…c31d4`，与项目清单记录一致，原文件未被修改。
- **全量巡览跟进（2026-09-17）**：全量真实点击巡览暴露多 Mesh 节点场景下的行为缺陷（真实产品缺陷，非巡览隔离）：迁移后 `moose.mesh_path` 无条件回落到第一个项目网格节点，活动网格未跟随自己的迁移目标（全量中前序步骤遗留的 `mesh_i02.msh` 节点抢占了活动网格）。修正 `migrate_project_mesh_paths()`：`moose.mesh_path` 命中迁移源时设为其迁移目标，仅当未参与迁移且不属于项目网格（含远程 snapshot 污染）时才回落到第一个项目网格。修复后全量 113 步巡览 exit=0。
- **状态**：已修复，人工回归通过

## 2026-09-19-025 MPa 换算显示暴露浮点伪精度

- **影响接口/模块**：材料表单快捷字段（弹性模量 MPa 显示），TASK-V02-014 专项评审发现。注：这是既有缺陷在评审中暴露，不是 v0.2 重构引入（表单显示代码本阶段未改动）。
- **现象**：G1 项目 Materials/concrete_elasticity（存储 `youngs_modulus=29791459780` Pa）的快捷字段“弹性模量 (MPa)”显示 `29791.459780000001`，期望 `29791.45978`。
- **复现步骤**：打开 G1 项目 → 选中 Materials/concrete_elasticity → 查看快捷表单“弹性模量 (MPa)”字段。
- **根因**：`src/PropertyEditor.cpp` 高级表→快捷字段回显（:641）与表单装载（:2625 附近）两处显示侧用 `QString::number(stored / factor, 'g', 17)`；17 位有效数字暴露了 double 商值的伪精度。
- **关联逻辑/路径**：`src/PropertyEditor.cpp`（显示路径两处）、新增 `src/UnitDisplay.cpp`、`tests/test_phase0.cpp::test_unit_display_contract`、巡览 `g1_isotropic_material_form_contract`。
- **修复方案**：新增 widget-free 工具 `gmp::format_unit_display_value()`（`include/gmp/UnitDisplay.h`），显示侧统一改用 15 位有效数字（'g',15）——不暴露伪精度，且显示值回解析 ×factor 后与存储值在双精度噪声内一致（15 位对显示足够；存储侧写回仍保留 'g',17 全精度，不动）。核对结果：'g',17 同款用法另有写回侧两处（display×factor 与 unit_factor_stress 元数据写入），属存储侧全精度语义，保持不变。样本显示：29791459780 Pa → `29791.45978`；206000000000 → `206000`；500 Pa → `0.0005`。
- **验证结果**：CTest 新增 `test_unit_display_contract`（显示文本精确等于 `29791.45978` 且无 `0000001` 尾巴、往返数值一致、单位键登记）；红/绿验证：改回 'g',17 时两条断言变红，还原后全绿；`g1_isotropic_material_form_contract` 扩展断言精确显示文本；`cdp_material_form`、`step_executioner_contract` 定向通过；全量 114 步巡览 exit=0。
- **状态**：已修复，人工回归通过（2026-09-19，014 专项评审中确认）

## 2026-09-19-026 高级参数表带单位键缺少单位提示

- **影响接口/模块**：材料表单快捷字段/高级参数表（propertyParamsTable）单位提示，TASK-V02-014 专项评审发现。注：既有缺陷，非 v0.2 重构引入。
- **现象**：同一对象的快捷字段显示 MPa 值、高级参数表显示 Pa 值，两栏数字不同且无单位说明，易误读。
- **复现步骤**：选中任一含 `youngs_modulus` 的材料节点 → 对比快捷字段（MPa）与高级参数表（Pa）两栏数字，均无单位说明。
- **根因**：带单位换算机制（display_unit_factors_/unit_factor_stress，实际仅覆盖 `youngs_modulus`）的键在高级参数表中没有单位提示；快捷字段部分标签缺单位文字。
- **关联逻辑/路径**：`src/PropertyEditor.cpp::unit_tooltip_for_param/load_from_item/set_param_value`、`src/UnitDisplay.cpp::unit_key_info`、`src/L10n.cpp` 字典、巡览 `g1_isotropic_material_form_contract`。
- **修复方案**：a) 高级参数表值单元格为已知单位键加 tooltip：“存储单位：Pa（SI 求解值）；快捷字段按 MPa 显示：<换算值>”（中英文均含单位符号，走现有语言分支）；装载与 `set_param_value` 更新两处都挂；值单元格可编辑文本不变，写回解析不受影响。b) 快捷表单标签补齐单位：Step 时间字段 start_time/end_time/dt/dtmin/dtmax 补 ` (s)` 并更新 L10n 字典；`youngs_modulus` 的“弹性模量 (MPa)”本已带单位；Loads 的共享 Factor 字段因跨载荷类型单位不同（Pressure=Pa、BodyForce 等不同）保持通用标签，不在本次务实范围内误标。tooltip 覆盖键清单：`youngs_modulus`（与 display_unit_factors_/unit_factor_stress 机制实际覆盖一致；后续接入新键在 `unit_key_info()` 登记）。
- **验证结果**：`g1_isotropic_material_form_contract` 扩展断言高级表 youngs_modulus 值单元格 tooltip 含 Pa/MPa 与换算值；`cdp_material_form`、`step_executioner_contract` 定向通过；CTest 全绿；全量 114 步巡览 exit=0。
- **状态**：已修复，人工回归通过（2026-09-19，014 专项评审中确认）

## 2026-09-19-027 新建项目后舞台残留旧项目网格

- **影响接口/模块**：项目生命周期（新建项目/打开项目）与 VTK 舞台/变量列表；RG-B 人工抽查发现。
- **定性结论**：**既有缺口，非 Stage 2 回归**。git 证据：`git diff ef6f0fd..HEAD -- src/MainWindow.cpp` 中新建项目路径（`create_fresh_project`）逐字未变，Stage 2 的 diff 只涉及 load/save 的 viewer settings 读写；`git show ef6f0fd:src/MainWindow.cpp` 中 `create_fresh_project` 同样不含任何 viewer 清理调用——重构前新建项目就不清理舞台。
- **现象**：打开 G1 项目（舞台显示装配网格、变量列表含 phys_id 等数组）→ 新建项目 → 中央舞台仍显示旧项目装配网格，变量列表残留旧网格数组；模型树已干净。
- **复现步骤**：1) 打开含网格的项目并确认舞台有内容；2) 新建项目；3) 观察舞台与变量列表未复位。打开无 viewer 段的项目（如老项目）也有同类残留。
- **根因**：`create_fresh_project` 从未调用 `viewer_->clear_stage_data()`；`load_project` 只在项目有 viewer 设置时 `apply_viewer_settings`，且 `apply_viewer_settings` 仅在 `current_file` 非空时加载新文件，否则旧舞台内容原样保留。
- **关联逻辑/路径**：`src/MainWindow.cpp::create_fresh_project/load_project`、`src/VtkViewer.cpp::clear_stage_data`、巡览 `project_context_isolation_contract`。
- **修复方案**：a) `create_fresh_project` 增加 `viewer_->clear_stage_data()`（清空网格/结果管线、变量数组列表、组筛选、拾取/切片态、文件引用）并清空 `active_ui_context_.stage_selections`（与 remove_item 的清理配对一致）；b) `load_project` 中目标项目无保存的 `current_file` 时先 `clear_stage_data()`，有则经 `apply_viewer_settings` 加载覆盖，无中间态闪存。
- **验证结果**：`project_context_isolation_contract` 扩展断言（新建前经 exodus fixture 让舞台持数 → 新建后 `has_stage_data()==false` 且 `current_file()` 为空；打开无 viewer 段项目同断言）；红/绿验证：摘除新建路径的 `clear_stage_data()` 后合同报 “New project retained the previous project's stage”，还原后全绿；`model_tree_projection_contract`（真实 G1 打开路径）、`project_reopen_group_contract`、`project_save_as_mesh_migration_contract` 定向通过；CTest 全绿；全量 114 步巡览 exit=0。019/024/G0 修复语义不回归。
- **状态**：已修复，待人工回归（RG-B 续验时确认）
