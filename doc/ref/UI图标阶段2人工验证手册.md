# 图标阶段②人工验证手册：工具栏 / 回放 / 舞台左侧工具条

> 日期：2026-09-24（18:30 修订：工具栏改纯图标、去视口标题）
> 代码：本地提交 `33db007` + 修订 `f9adac0`（未推送）
> 范围：阶段②——主窗口工具栏 6 组、回放控制组、Display 悬浮组、舞台左侧工具条（模型树和对话框属阶段③④）

## 修订后检查要点（覆盖原 1.1/1.4 节）

- 工具栏 6 组为**纯图标**（16px、行高 30px），一排应完整显示到最右的回放组，无裁切；
- **悬停每个图标按钮应出中文 tooltip**（重点：新建/打开/保存/另存/截图/撤销/重做/同步/生成网格/预览网格/导入/运行/检查/停止）；
- 中央视口区顶部的「视口」文字标题已删除，工具条直接顶到模块栏。

### 1.2 回放控制组（纯图标，悬停出中文 tooltip）

▶ 播放 / ❚❚ 暂停 / ■ 停止 / ⏮ 上一步 / ⏭ 下一步——纯图标无文字，**悬停应显示中文 tooltip**（播放时间步动画/暂停时间步动画/停止播放并回到第一个时间步/后退一个时间步/前进一个时间步）。需打开一个 Exodus 结果拖时间步验证按钮可用性。

### 1.3 舞台左侧工具条（原自绘图标已全换 FA）

从上往下：收起(«)/旋转/平移/缩放、拾取/清除、适配/前/右/顶/等轴测视图、显示模式/切片、草图六工具（与草图编辑器同字形）、网格/生成网格、可视化/结果。

检查点：① 图标风格与其余界面统一（深灰、实心）；② 收起大按钮点击后面板收起、图标变为 »；③ 所有按钮 tooltip 与功能不变。

### 1.4 全局一致项

- 选中/激活态图标为高亮蓝 `#2f6fed`（与播放进度条滑块同色）——可勾选按钮处验证；
- 菜单里「校验工作流」也带上了剪贴板勾选图标（帮助 › 或直接看菜单）。

## 2. 重点裁定（阶段①遗留 + 本阶段新增）

| 位置 | 当前字形 | 意见 |
|---|---|---|
| 工具栏 同步模型到 MOOSE 输入 | arrows-spin（旋转环） | 阶段①手册 §4 遗留：接受或换（如 `file-export`/`gears`） |
| 视图按钮 前/右/顶 | 向下/向右/向上箭头 | 单箭头较弱，备选：三张视图用 `square`+方向组合或保留 |
| Display 组 切片 | layer-group（叠层） | 与放样/布尔合并同形，若介意可换 `cut`（剪刀）语义 |

## 3. 反馈格式

逐项「通过/不通过」；不通过注明：区域、按钮、现象（裁切/缺失/错位/语义不合/功能异常）。

## 4. 已做自动验证

- CTest 1/1 通过；
- `playback_controls_contract`（回放组合同）、`stage_command`×12（舞台左侧工具条驱动）、工具栏场景 S1~S6、`operation_log_smoke` 全部通过；
- 截图抽查：工具栏文字无裁切、图标清晰。
