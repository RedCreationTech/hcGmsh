# 样式修复第二批 2C 人工验证手册：命名与术语统一

> 日期：2026-09-26
> 代码：本地提交 `6b9d7a0`（未推送，待验收后全量巡览 + 推送）

## 1. Mesh 窗参数命名（N1）

几何 › 变换页：平移 X/Y/Z、旋转原点 **Pivot** X/Y/Z、旋转轴 **Axis** X/Y/Z、缩放中心 **Center** X/Y/Z、缩放系数 **Factor** X/Y/Z；基元页：原点 **Origin** X/Y/Z、尺寸 Size/Axis X/Y/Z——不再有四套混用。

## 2. 「维度」取值格式（N2）

全部维度下拉统一为：全部 / 0 — 点 / 1 — 线 / 2 — 面 / 3 — 体（涉及 Mesh 窗六个下拉 + Visualization 网格页）。**功能验证**：按维度筛选/生成网格行为不变（只改了显示文字）。

## 3. 重名消除（N3）

| 位置 | 旧 | 新 |
|---|---|---|
| Visualization › 标量页字段标签 | 标量（与分组头重名） | **变量** |
| Visualization › 时间页 | 分组头+行首两个「时间」 | 行首标签已去 |
| Visualization › 视图页按钮 | 重置过滤器（与「重置」下拉近似） | **清除过滤器** |
| 曲线清单表头 | 曲线（与页签重名） | **名称** |

## 4. Part 特征 TAB（N4）

拉伸/旋转/放样/扫掠四页切换时，标签列宽一致、输入框起始位置不再跳动（放样/扫掠的长标签「第一截面/轮廓草图」为基准）。

## 5. Job 三层页签（N5）

MOOSE 设置 › 输入文件 › 子页签（生成输入/自定义块/生成报告）字号变小、弱化，与外层页签一眼可分层级。

## 6. 术语（N6）

- Visualization › 变形页「Vector Arrays」→「向量数组」；
- Mesh 窗一级页签「分组与网格场」→「**物理组与网格场**」；
- 用户可见的「舞台」→「视口」（约 10 处：VtkViewer 状态反馈、Gmsh 实体选择帮助、对比窗 tooltip）。

## 7. 遗留告知（实施方发现，不在本批范围）

- 对比窗帮助英文源串仍含 "on stage"（需改 MainWindow.cpp，未授权本批）；
- Gmsh 实体选择帮助里曲线仍译「边」（与 N2 的「线」并存，建议下批统一）。

## 8. 已做自动验证

results_workspace / i04_work_window / remote_job_monitor / l10n 巡览、audit_ 43/43、CTest 全过；变换页命名截图目检。

通过后我跑全量巡览并推送，**第二批（2A/2B/2C）即全部完成**，剩余第三批（编辑器/表单风格融合、禁用态统一、术语中文化映射表）按计划在 V02 前评估是否继续。
