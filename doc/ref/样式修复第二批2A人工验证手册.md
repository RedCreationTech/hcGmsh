# 样式修复第二批 2A 人工验证手册：按钮规范

> 日期：2026-09-26
> 代码：本地提交 `800817a`（未推送，待验收后全量巡览 + 推送）

## 1. 按钮文案精简（9 模块页）

逐个打开左侧模块页，对照预期（全部带图标，文字变短）：

| 页 | 预期文案 |
|---|---|
| 通用底部行（9 页） | **打开所选**（不再带 部件/材料/… 后缀）、重命名、复制、删除、刷新 |
| Part | 打开根节点、新建部件、网格面板 |
| Property | 应用所选、添加、移除、应用模板 |
| Material | 打开根节点、新建材料、CDP 材料、属性编辑器 |
| Section | 打开根节点、新建实体截面、属性编辑器 |
| Step | 打开根节点、静力步、瞬态步、预设: steady |
| Interaction | 打开根节点、相互作用、面接触 |
| Load | 打开根节点、通用载荷、面压力、边界条件、热源 |
| Sketch / Assembly | 不变（第一批已规范） |

**English 切换**：应显示英文原文（Open Selected part / Add Static Step…），无中文残留。

**留待你裁决的边界项**（实施方未擅自改）：Property 页「应用所选边界/应用所选分组」、Step 页「添加稳态分析步」保持原样；如需一并精简告知即可。

## 2. Results 页分组

结果页顶部单行三组，组间竖线分隔：
- 常规：打开根节点、刷新列表、导入结果文件…、导入任务目录…、验证结果包
- 查看：在视图中打开、预览、以文本打开、新建对比窗口
- 维护：**移到废纸篓…（红字红边警示）**、重新扫描…、重新定位…

## 3. 曲线页菜单化

- 常驻只有 4 个：固定预览曲线、导入 CSV…、**导出 ▾**、**曲线 ▾**
- 导出 ▾ 菜单：PNG / SVG / CSV / 复制图像；曲线 ▾ 菜单：移除所选 / 编辑所选 / 比较两条曲线
- 各菜单项点击功能与原按钮一致（抽测 1~2 个）
- 无数据时两个菜单按钮应**禁用**（打开空结果页验证灰色）

## 4. Mesh 窗按钮宽度

- 几何 › 布尔：两个「拾取」为小按钮、与各自输入框同行（不再通栏独占一行）
- 分组与网格场 › 物理组：「刷新」为小按钮（与网格场页规格一致）

## 5. 已做自动验证

results_workspace_contract（曲线页合同，含固定预览断言）、v02_l10n_round_trip、module_01/06/08、audit_ 43/43、CTest 全过；Results 分组与曲线页菜单截图目检通过。

反馈通过或圈改；通过后我跑全量巡览并推送，然后继续 2B（空态统一）。

---

## 7. 验收反馈修正（2026-09-26 晚，本地 `0e09f08`+`bfedf78`）

| # | 反馈 | 修复 |
|---|---|---|
| 补1 | Results 按钮一行显示不开出横向滚动条 | 改两行分组：上行=常规+类型筛选；下行=查看｜维护 |
| 补2 | 数值输入框偏矮不协调 | 全局统一高度基线（按钮/输入框/下拉同一度量） |
| 补3 | 移到废纸篓按钮高度不一致 | 同一基线顺带解决 |
| 补4 | 结果表格不按容器宽拉伸、右侧留白 | 末列拉伸自适应 |
| 补5 | 曲线清单「可见」列旁滚动条残影 | 根因=横/纵滚动条互相反馈；横向滚动条关闭（列宽由内容+末列拉伸管理） |
| 补6 | 弹窗按钮整体偏高 | 高度收矮一档（padding 3px/10px、min-height 18/22px） |

复查重点：Results 页两行布局与窄窗表现、曲线清单残影消失、按钮高度手感、表格拉伸。
