# 样式修复第一批人工验证手册

> 日期：2026-09-26
> 代码：本地提交 `deb102f`（未推送，待本批人工验收后全量巡览 + 推送）
> 范围：漏翻修复 ×4、布局 bug ×3、草图页重做、拾取改名（见 `doc/ref/样式修复第一批改动计划.md`）

## 1. 漏翻修复

| # | 检查点 | 预期 |
|---|---|---|
| 1.1 | 装配模块页底部第一个按钮 | 「打开所选实例」（不再是英文 `Open Selected instance`） |
| 1.2 | Job › MOOSE 设置 › 算例设置 | 三个输入框占位符全中文：自动检测 MOOSE 可执行文件／输入文件路径 (\*.i)／工作目录 (可选)；执行配置页作业行无任务时显示「（尚未提交作业）」；自定义块编辑器占位符中文（示例代码保留英文） |
| 1.3 | 属性编辑器/浮动表单 › 校验 TAB 空态 | 两处均为中文「无校验问题。」（不再一处英文一处中文） |
| 1.4 | 分析步模块页序列预览框 | 中文文案：执行器只使用第一个分析步；其余仅供检查／已配置的序列：／`1) transient \| 类型=Transient 时间步=0.01 结束时间=0.2` |

**切换 English 复查一遍**：上述位置应回切英文，无中文残留。

## 2. 布局 bug

| # | 检查点 | 预期 |
|---|---|---|
| 2.1 | Visualization › 网格页 | 组/实体/类型三个下拉框完整显示在滚动区内，右缘不再被切断；横向无滚动条 |
| 2.2 | Mesh › 模型页 | 「使用示例盒」复选框与下方「尺寸 X/Y/Z」左对齐（不再右缩进） |
| 2.3 | Job › MOOSE 设置 › 执行配置 | 「使用 mpiexec」复选框与 MPI 进程数等字段左对齐 |

## 3. 草图页重做

打开 Sketch 模块页（管理态）：

- 顶部：新建草图、打开草图根节点（两个左对齐小按钮）
- 中部：当前条目列表**撑满整个窗口高度**（不再 40% 空白）
- 底部：打开所选草图／重命名／复制／删除 + 右侧刷新
- 功能抽测：新建草图 → 列表出现 → 选中后点「打开所选草图」进入编辑；「打开草图根节点」点后左侧模型树选中 Sketches 根

## 4. 拾取改名

Job › MOOSE 设置 › 算例设置：可执行文件/输入文件/工作目录三个按钮显示「浏览…」（文件夹图标），点击弹出文件选择对话框；Mesh 窗的视口「拾取」按钮保持原名原图标不变。

## 5. 反馈格式

逐项「通过/不通过 + 现象」。通过后我将跑全量巡览 + CTest 并推送。

## 6. 已做自动验证

- 定向巡览：sketch_ 10 步（1 步为已知顺序依赖夹具，全量中正常）、step_executioner、v02_l10n、remote_job_monitor、cdp_material_form、i04_×3、audit_ 43/43；
- CTest 1/1；
- 截图目检：草图页新布局、网格页下拉完整、算例设置中文占位符。
