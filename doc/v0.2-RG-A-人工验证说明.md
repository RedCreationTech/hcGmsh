# v0.2 RG-A（Stage 0 准出）人工验证说明

> 日期：2026-09-18
> 范围：Stage 0（TASK-V02-001～004）准出抽查
> 验证人：用户（按任务清单 Q7 批复：Stage 闸门抽查 15～30 分钟）
> 通过后即可进入 Stage 1（hc_core 引入）

## 0. 本次重构改了什么（为什么风险低）

Stage 0 只动了**测试与文档**，产品代码零净变更：

- `doc/v0.2-回归基线.md`：自动化基线 + G1 证据链 + 等价判据 + 性能基线（§5）；
- `doc/schema/project-v2.md`：补 `Input Cases`（修文档漂移 A-019）；
- `tests/test_phase0.cpp`：新增根节点冻结清单合同测试、物理组清单合同测试、快照 v2 manifest 合同测试。

自动化状态：全量真实点击巡览 113 步全绿（exit=0，无 FAILED）；CTest 全绿。

## 1. 验证步骤（约 15 分钟）

### 1.1 构建与启动

```bash
cd /Users/a123/Desktop/code/Gmsh-moose-parview
cmake --build build -j4
./build/gmp_ise
```

### 1.2 G1 主干抽查（行为与基期一致）

1. 打开 `/Users/a123/Desktop/test/moose/workspace/0915/phase5-g1-assembly-contact.gmp.yaml`；
2. 确认标题栏显示项目文件名；状态区 `Workflow ready`；
3. 确认装配预览正常显示（两实体、多色物理组着色）；
4. 切到“网格”模块，打开 `mesh_assembly_g1` 的 Mesh Workspace，确认物理组统计表 8 个组齐全、单元数与基线一致（instance_plate 67510 / instance_concrete 85440 / fixed_bottom 8544 / load_top 27004 / contact_concrete 8544 / contact_plate 27004）；
5. 点“同步模型到输入”，确认 `.i` 内容与基期一致（重点：`[Contact]`、`[BCs]` 四个子块、`[Materials]` 四件、单一 `[Executioner]`）；
6. 再点一次同步，确认文本无变化（A/B 一致性抽查）；
7. 保存项目，确认标题栏与保存提示正常。

### 1.3 性能抽查（对照 `doc/v0.2-回归基线.md` §5）

| 操作 | 基线 | 本次感受 |
|---|---:|---|
| 打开 G1 项目 | ≈30 ms | 应“瞬时” |
| 同步生成 `.i` | ≈15 ms | 应“瞬时” |
| 保存项目 | ≈20 ms | 应“瞬时” |

（本阶段未重新生成网格则无需测 3.2s 那一项；如顺手重新生成，确认仍在秒级。）

### 1.4 交互质感抽查（巡览覆盖不到的点）

- 双击任意节点打开/关闭浮动表单，确认模态行为、确定/取消语义不变；
- 切换模块（部件/网格/属性/作业），确认工具组、树选择、舞台联动无异常；
- 拖动一个工具组到舞台右上角再拖回，确认磁吸/浮动正常。

## 2. 通过标准与回填

- 以上全部正常 → RG-A 通过，回复我“RG-A 通过”，我回填任务清单并进入 Stage 1；
- 任何异常 → 描述现象（最好带截图/日志），我登记差异、修复后重新申请 RG-A。

## 3. 相关提交

- `0b8cd44` 固化回归基线（TASK-V02-001）
- `fae055a` Input Cases 文档 + 根节点合同测试（TASK-V02-002，关闭 A-019）
- `d0fdd5e` 性能基线采集（TASK-V02-004）
- `5163535` G1 链路服务级测试（TASK-V02-003）
- `ff62fc4` 执行记录回填
