# TASK-V02-014 专项评审说明（Tree→Projection，A-002 闸门）

> 日期：2026-09-18
> 提交：`2ea6d09`（适配器层）、`87ffc7c`（接线层）、`7bee5e0`（执行记录）
> 评审人：用户（任务清单 Stage 1 规定：014 后独立专项评审，通过后才进 Stage 2）
> 预计耗时：20～30 分钟

## 0. 本次改了什么

建立了 `ModelTreeAdapter`（`gmp::core`）：ProjectDocument 成为运行时工程真源，模型树 QTreeWidget 退为投影。采用**双写并存 + 懒同步**：

- 树仍是唯一操作入口，继续承载 Data Role 数据（所有既有巡览合同不受影响）；
- 树的每次变更标脏，Document 在读取时全量重建；
- ObjectId 为确定性路径 ID（`根名/子节点名`），同一项目两次加载 ID 一致，不用随机 UUID；
- Document 不参与持久化，`.gmp.yaml` schema v2 格式不变。

自动化证据：新增巡览步骤 `model_tree_projection_contract`（基线 113→114，含真实 G1 项目等价判据）；全量巡览 114 步全绿；CTest 全绿（168 条断言）。

## 1. 评审操作（重点路径逐条过）

### 1.1 构建与启动

```bash
cd /Users/a123/Desktop/code/Gmsh-moose-parview
git pull  # 应已在 7bee5e0 或更新
cmake --build build -j4
./build/gmp_ise
```

### 1.2 属性表单编辑闭环（信号阻塞路径是重点）

1. 打开 G1 项目 `phase5-g1-assembly-contact.gmp.yaml`；
2. 双击 `Materials/concrete_elasticity`，修改 Young's Modulus（MPa）一个可恢复的值，点“确定”；确认树状态与项目脏标记正常；
3. 重新双击打开，确认值已写入；改回原值并确定；
4. 再打开任意节点，修改后点“**取消**”，重开确认值未变；
5. 保存 → 关闭 → 重开项目，确认参数与基线一致。

### 1.3 结构变更传播

1. 重命名一个可恢复的节点（如把某 BC 改名再改回），确认下游 stale 标记与模块列表刷新正常；
2. 新建一个临时 BC 节点再删除，确认树状态与工作流校验正常；
3. 复制一个节点再删除副本。

### 1.4 项目生命周期全流程

1. **重开 G1 项目**：确认不点“构建装配”直接“校验工作流”即为 ready（019 修复语义不回归）；
2. **另存为副本**：另存为临时副本（如 `/tmp/v02-014-review-copy.gmp.yaml`），确认副本 Mesh 路径迁移到副本自有工作目录（024 修复语义不回归），同步 `.i` 的 `[Mesh/file]` 指向副本路径；关闭副本；
3. **新建项目**：新建一个临时项目，确认不继承上一个项目的 input/workdir/mesh 路径（G0 上下文隔离语义不回归）；关闭不保存。

### 1.5 G1 主干等价抽查

1. 重开 G1 项目，同步 `.i` 两次，文本应逐字一致（`[Contact]` 含 `primary = contact_plate`）；
2. 装配预览、物理组清单、生成报告与基线一致。

### 1.6 性能观感

打开 G1 项目、同步 `.i`、保存应与基线（≈30ms / ≈15ms / ≈20ms）无体感差异。

## 2. 通过标准与回填

- 全部正常 → 回复“014 专项评审通过”，我回填记录并进入 Stage 2（ProjectStore 抽离）；
- 任何异常 → 描述现象（截图/日志），我登记差异、修复后重新申请评审。

## 3. 已知边界（不算缺陷）

- Document 读取时全量重建投影：G1 规模（约 30 节点）无感；节点数上千的性能问题留待后续评估；
- 重命名在 Document 中表现为“旧 ID 消失 + 新 ID 出现”（路径 ID 语义的必然结果），持久化与追溯不受影响。
