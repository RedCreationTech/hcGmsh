# CAE 只连接 LIMS Facade，不直接连接 C06 Agent，也不保存 C06 令牌。
# LIMS 与 CAE 在同一台电脑运行时无需随 121/138 切换修改此值。
GMP_LIMS_BASE_URL=http://127.0.0.1:8200

# 可选：显式指定其他 .env 文件；通常不需要。
# GMP_ENV_FILE=/absolute/path/to/gmp-ise.env
